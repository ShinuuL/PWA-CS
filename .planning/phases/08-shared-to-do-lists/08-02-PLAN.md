---
phase: 08-shared-to-do-lists
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - FRONTEND/src/features/agenda/ListsTab.jsx
  - FRONTEND/src/features/agenda/ListsTab.css
  - FRONTEND/src/features/agenda/ListCard.jsx
  - FRONTEND/src/features/agenda/ListCard.css
  - FRONTEND/src/features/agenda/ItemRow.jsx
  - FRONTEND/src/features/agenda/ItemRow.css
  - FRONTEND/src/features/agenda/ListForm.jsx
  - FRONTEND/src/features/agenda/ListForm.css
  - FRONTEND/src/features/agenda/ItemForm.jsx
  - FRONTEND/src/features/agenda/ItemForm.css
  - FRONTEND/src/features/agenda/AgendaPage.jsx
autonomous: true
requirements:
  - TODO-01
  - TODO-02
  - TODO-03
  - TODO-04
  - TODO-05
  - TODO-06
  - TODO-07
  - TODO-08

must_haves:
  truths:
    - "AgendaPage renders 4 tabs: Eventos, Lembretes, Notas, Listas (per D-01)"
    - "AgendaPage initializes todoStore on mount and cleans up on unmount"
    - "ListsTab shows empty state with List icon + 'Nenhuma lista' heading + 'Crie sua primeira lista compartilhada' body + 'Criar lista' CTA when no lists exist"
    - "ListsTab shows 2-column grid of ListCards when lists exist (per D-04)"
    - "Each ListCard displays list name with text-overflow ellipsis, color accent line at top, item count (e.g., '3/5 concluidos'), and completion progress"
    - "Long list names truncate with text-overflow ellipsis and do not break card layout"
    - "ListsTab shows loading skeleton with 3 placeholder cards and agenda-pulse animation"
    - "ListsTab shows error state with 'Algo deu errado — Tente novamente' text and 'Tentar novamente' retry button"
    - "Selecting a ListCard enters that list's item view with horizontal scroll chips at top (per D-02)"
    - "Item view shows compact rows: checkbox | title | assignee badge (mini avatar) | due date"
    - "Completed items get strikethrough text, fade opacity, and move to collapsible 'Concluidos' section (per D-12, D-15)"
    - "Collapsible 'Concluidos' section shows count badge when collapsed (per D-15)"
    - "Overdue items show red date text and sort to top of their group (per D-11)"
    - "Items sort by due date first (items with dates first, then undated), then by creation order (per D-21)"
    - "ListForm modal allows creating a list with name (required) and color picker (per D-07)"
    - "ListForm modal allows renaming an existing list (per D-06)"
    - "ItemForm modal allows creating an item with title (required), assign to Me/Partner toggle, and optional due date via DateTimePicker (per D-09)"
    - "ItemForm modal allows editing an existing item with all fields pre-filled (per D-14)"
    - "FAB opens ListForm for list creation and ItemForm for item creation (per D-05)"
    - "Delete list shows confirmation dialog: 'Excluir [list name]? Todos os itens serao removidos.' with Cancel/Excluir buttons (per D-08)"
    - "Delete item shows confirmation dialog: 'Excluir este item?' with Cancel/Excluir buttons"
    - "Assignee badge displays mini avatar of assigned user; unassigned items show no badge (per D-16, D-17)"
    - "Edit mode toggle at top of list shows checkboxes for bulk select + delete button (per D-13)"
    - "Empty state for no items in selected list shows 'Nenhum item' heading + 'Adicione o primeiro item a esta lista' body"
    - "Loading state for items shows 3 skeleton rows with agenda-pulse animation"
    - "Zero-one-many: zero lists = empty state, one list = single card, many lists = 2-column grid"
    - "Item list container uses overflow-y: auto; completed section capped at max-height: 400px with overflow scroll"
  artifacts:
    - FRONTEND/src/features/agenda/ListsTab.jsx
    - FRONTEND/src/features/agenda/ListsTab.css
    - FRONTEND/src/features/agenda/ListCard.jsx
    - FRONTEND/src/features/agenda/ListCard.css
    - FRONTEND/src/features/agenda/ItemRow.jsx
    - FRONTEND/src/features/agenda/ItemRow.css
    - FRONTEND/src/features/agenda/ListForm.jsx
    - FRONTEND/src/features/agenda/ListForm.css
    - FRONTEND/src/features/agenda/ItemForm.jsx
    - FRONTEND/src/features/agenda/ItemForm.css
  key_links:
    - "AgendaPage tabs array includes { id: 'lists', label: 'Listas' } as 4th tab"
    - "AgendaPage calls todoStore.initializeTodos(pair.id) on mount"
    - "ListsTab imports and uses todoStore for all data operations"
    - "ListForm imports ColorPicker for list color selection"
    - "ItemForm imports DateTimePicker from Phase 7 for due date selection"
    - "ItemRow resolves assigned_to relative to creator for correct badge display"
  prohibitions: []

---

<objective>
Build the complete ListsTab UI for shared to-do lists, integrating with the todoStore from Plan 1. This plan creates all UI components (ListsTab, ListCard, ItemRow, ListForm, ItemForm), adds the "Listas" tab to AgendaPage, and delivers the full user-facing to-do list experience with list management, item CRUD, real-time updates, color personalization, and assignee badges.

Purpose: This is the user-facing layer. After this plan, couples can create named lists with colors, add/complete/delete items, assign items to each other, set due dates, and see everything update in real time. All 8 TODO requirements are delivered.

Output: Working ListsTab with full CRUD, color picker, assignee badges, due dates, sorting, collapsible completed section, and real-time sync.
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-shared-to-do-lists/08-CONTEXT.md
@.planning/phases/08-shared-to-do-lists/08-RESEARCH.md
@.planning/phases/08-shared-to-do-lists/08-UI-SPEC.md
@.planning/phases/08-shared-to-do-lists/08-01-SUMMARY.md
@FRONTEND/src/features/agenda/AgendaPage.jsx
@FRONTEND/src/features/agenda/RemindersTab.jsx
@FRONTEND/src/features/agenda/RemindersTab.css
@FRONTEND/src/features/agenda/EventsTab.jsx
@FRONTEND/src/features/agenda/DateTimePicker.jsx
@FRONTEND/src/stores/todoStore.js
@FRONTEND/src/features/agenda/ColorPicker.jsx
@FRONTEND/src/stores/authStore.js
@FRONTEND/src/shared/lib/supabase.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ListsTab + ListCard + AgendaPage Integration</name>
  <files>FRONTEND/src/features/agenda/ListsTab.jsx, FRONTEND/src/features/agenda/ListsTab.css, FRONTEND/src/features/agenda/ListCard.jsx, FRONTEND/src/features/agenda/ListCard.css, FRONTEND/src/features/agenda/AgendaPage.jsx</files>
  <read_first>
    - FRONTEND/src/stores/todoStore.js — available actions and state shape (from Plan 1)
    - FRONTEND/src/features/agenda/RemindersTab.jsx — exact pattern for FAB, modal overlay, collapsible completed section, loading skeleton, error state, empty state
    - FRONTEND/src/features/agenda/RemindersTab.css — CSS variable patterns for skeleton, modal, FAB, completed section
    - FRONTEND/src/features/agenda/EventsTab.jsx — FAB + modal pattern confirmation
    - FRONTEND/src/features/agenda/AgendaPage.jsx — current 3-tab structure, tabs array, store initialization pattern
    - FRONTEND/src/stores/authStore.js — how to get current user for assignee resolution
    - FRONTEND/src/shared/lib/supabase.js — supabase client for profile fetching
    - FRONTEND/src/features/agenda/agenda.css — SegmentedTabs styles, shared agenda layout
  </read_first>
  <behavior>
    - ListsTab renders empty state when lists array is empty
    - ListsTab renders 2-column grid of ListCards when lists exist
    - Clicking a ListCard enters item view with that list's items
    - ListCard shows name (truncated), color accent, item count, completion percentage
    - FAB opens ListForm modal for creating a new list
    - AgendaPage has 4 tabs and initializes todoStore
  </behavior>
  <action>
    1. Create `FRONTEND/src/features/agenda/ListCard.jsx` and `ListCard.css`:
       - Props: list, itemCount, completedCount, onClick
       - Renders a card with: color accent line (4px bar at top using list.color or var(--color-primary)), list name (with text-overflow ellipsis, white-space nowrap, overflow hidden), item count text (e.g., "{completedCount}/{itemCount} concluidos"), subtle progress bar or completion ratio
       - Card uses --color-bg-card background, var(--radius-lg) corners, padding: var(--space-md)
       - Hover/tap feedback with subtle scale or opacity transition
       - Per D-04: 2-column grid card, D-18: completion count text below name

    2. Create `FRONTEND/src/features/agenda/ListsTab.jsx` and `ListsTab.css`:
       - Import useTodoStore, ListCard, ListForm, ItemForm, ColorPicker (from Plan 1)
       - Import Plus, List, Check, ChevronDown, ChevronRight, Trash2 from lucide-react
       - State: activeListId (null = grid view, string = item view), showListForm, showItemForm, editItem, completedExpanded, editMode, selectedItems (Set), profiles
       - Fetch profiles for assignee badges: useEffect on items, fetch unique created_by IDs from profiles table (same pattern as RemindersTab)
       - Loading state: 3 skeleton cards with agenda-pulse animation
       - Error state: "Algo deu errado — Tente novamente" + retry button
       - Empty state (no lists): List icon (size 40) + "Nenhuma lista" heading + "Crie sua primeira lista compartilhada" body + "Criar lista" CTA button (per D-03)
       - Grid view: 2-column CSS grid of ListCards. onClick sets activeListId
       - Selected list view: horizontal scroll chips at top (per D-02). Back button to return to grid. FAB for adding items
       - Chips: each chip shows list name with color dot, active chip highlighted. "+" chip to create new list
       - Item view: sorted items from todoStore (use useMemo for sort by due date per D-21, D-06). Active items on top, collapsible "Concluidos" section at bottom
       - Collapsible "Concluidos" section: button with count badge, ChevronDown/ChevronRight toggle, collapsed by default (per D-15)
       - Completed items: strikethrough text, opacity 0.6, inside collapsible section (per D-12)
       - Overdue items: red date text (#FF6B6B) when due_at < now and not completed (per D-11)
       - FAB: Plus icon, onClick opens ItemForm for creating new item (per D-05)
       - Edit mode toggle: button at top of item view, toggles editMode state. Shows checkboxes for bulk select + delete button (per D-13)
       - Bulk delete: when items selected in edit mode, show delete count and delete button
       - ItemRow integration: render ItemRow for each item with appropriate props
       - ListForm modal: when showListForm is true, render modal overlay with ListForm inside. Pass editList (null for create, list object for rename)
       - ItemForm modal: when showItemForm is true, render modal overlay with ItemForm inside. Pass editItem (null for create, item object for edit)
       - Delete list: confirmation dialog per D-08, then call todoStore.deleteList
       - Empty items state: "Nenhum item" heading + "Adicione o primeiro item a esta lista" body

    3. Update `FRONTEND/src/features/agenda/AgendaPage.jsx`:
       - Import useTodoStore and ListsTab
       - Add { id: 'lists', label: 'Listas' } as 4th tab in tabs array (per D-01)
       - Add todoStore.initializeTodos and todoStore.cleanup in useEffect (same pattern as reminderStore)
       - Add conditional render: {activeTab === 'lists' && <ListsTab />}
  </action>
  <verify>
    <automated>cd FRONTEND && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - When AgendaPage renders, 4 tabs are visible: Eventos, Lembretes, Notas, Listas
    - When "Listas" tab is active, ListsTab renders
    - When no lists exist, empty state shows icon + heading + body + CTA
    - When lists exist, 2-column grid of ListCards renders
    - When a ListCard is clicked, item view opens with chips at top
    - ListCard displays list name (truncated), color accent, item count
    - FAB opens ListForm modal for creating a new list
    - Collapsible "Concluidos" section shows completed items with count badge
    - Loading skeleton renders 3 placeholder cards
    - Error state shows retry message and button
    - Edit mode toggle shows bulk select checkboxes
    - Delete list shows confirmation dialog per D-08
    - Empty items state shows "Nenhum item" heading
    - `npm run lint` passes with 0 errors
  </acceptance_criteria>
  <done>ListsTab renders complete to-do list UI with grid view, item view, FAB, modals, collapsible completed section, edit mode; AgendaPage has 4 tabs and initializes todoStore</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ItemRow + ListForm + ItemForm</name>
  <files>FRONTEND/src/features/agenda/ItemRow.jsx, FRONTEND/src/features/agenda/ItemRow.css, FRONTEND/src/features/agenda/ListForm.jsx, FRONTEND/src/features/agenda/ListForm.css, FRONTEND/src/features/agenda/ItemForm.jsx, FRONTEND/src/features/agenda/ItemForm.css</files>
  <read_first>
    - FRONTEND/src/features/agenda/ListsTab.jsx — just created, consumes these components
    - FRONTEND/src/stores/todoStore.js — available actions (createItem, toggleItem, updateItem, deleteItem, createList, updateList)
    - FRONTEND/src/features/agenda/ColorPicker.jsx — from Plan 1, used in ListForm
    - FRONTEND/src/features/agenda/DateTimePicker.jsx — from Phase 7, used in ItemForm for due date
    - FRONTEND/src/stores/authStore.js — get current user for assignee resolution
    - FRONTEND/src/shared/lib/supabase.js — fetch partner profile for avatar
    - FRONTEND/src/features/agenda/ReminderCard.jsx — pattern for card styling
    - FRONTEND/src/features/agenda/ReminderForm.jsx — pattern for modal form with submit/cancel
  </read_first>
  <behavior>
    - ItemRow shows checkbox, title, assignee badge, due date in a compact row
    - Checking the checkbox toggles item completed state
    - ListForm accepts name and color, validates name required, submits on Enter
    - ItemForm accepts title, assigned_to, due_at, validates title required
    - ColorPicker in ListForm allows picking a hex color for the list
    - DateTimePicker in ItemForm allows selecting an optional due date
  </behavior>
  <action>
    1. Create `FRONTEND/src/features/agenda/ItemRow.jsx` and `ItemRow.css`:
       - Props: item, profiles, onToggle, onEdit, onDelete, currentUser, partnerId, editMode, selected, onSelect
       - Layout: flex row with checkbox | title | assignee badge | due date
       - Checkbox: custom styled checkbox (not native), calls onToggle(item.id) on change. Checked state from item.completed
       - Title: text with strikethrough when completed, opacity 0.6 when completed
       - Assignee badge (per D-16, D-17): if item.assigned_to is null, render nothing. If assigned_to is 'me', resolve to currentUser if item.created_by === currentUser.id, else partnerId. If 'partner', resolve to the other user. Render mini avatar (24px circle) from profiles map. If no avatar_url, show initial letter in colored circle
       - Due date: format with date-fns 'd MMM' in ptBR locale. If overdue (due_at < now && !completed), color: #FF6B6B (per D-11). If no due_at, show nothing
       - Edit mode: when editMode is true, show checkbox for selection. selected prop controls visual highlight
       - Row click: in normal mode, calls onEdit(item). In edit mode, calls onSelect(item.id)
       - CSS: flex layout, align-items center, gap 8px, padding 12px 16px, border-bottom 1px solid var(--color-border), background var(--color-bg-card), border-radius var(--radius-md)

    2. Create `FRONTEND/src/features/agenda/ListForm.jsx` and `ListForm.css`:
       - Props: onSubmit, onCancel, initialList (null for create, object for rename)
       - State: name (from initialList?.name || ''), color (from initialList?.color || '#B87CFF')
       - Form: text input for name (required, placeholder "Nome da lista"), ColorPicker for color selection
       - Submit button: "Criar lista" (create) or "Salvar" (edit)
       - Cancel button: "Cancelar"
       - Validation: name must not be empty/whitespace
       - On submit: call onSubmit({ name: name.trim(), color })
       - CSS: modal form layout matching ReminderForm pattern, input styled with --color-bg-input

    3. Create `FRONTEND/src/features/agenda/ItemForm.jsx` and `ItemForm.css`:
       - Props: onSubmit, onCancel, initialItem (null for create, object for edit)
       - State: title (from initialItem?.title || ''), assigned_to (from initialItem?.assigned_to || null), due_at (from initialItem?.due_at || null)
       - Form: text input for title (required, placeholder "Titulo do item"), assignee toggle (Me/Partner/None buttons), DateTimePicker for optional due date
       - Assignee toggle: 3-state button group — "Eu" (me), "Parceiro" (partner), "Nenhum" (null). Highlight active state
       - Due date: render DateTimePicker component from Phase 7. Include a "Remover data" button when due_at is set
       - Submit button: "Criar item" (create) or "Salvar" (edit)
       - Cancel button: "Cancelar"
       - Validation: title must not be empty/whitespace
       - On submit: call onSubmit({ title: title.trim(), assigned_to, due_at })
       - CSS: modal form layout matching ListForm pattern
  </action>
  <verify>
    <automated>cd FRONTEND && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - ItemRow renders checkbox, title, assignee badge, and due date in a horizontal row
    - When checkbox is clicked, onToggle is called with item ID
    - When item is completed, title has strikethrough and row has reduced opacity
    - When assigned_to is 'me' and current user is creator, badge shows current user's avatar
    - When assigned_to is 'partner' and current user is creator, badge shows partner's avatar
    - When assigned_to is null, no badge is rendered
    - Overdue items show red date text
    - ListForm validates name is required before submit
    - ListForm ColorPicker allows selecting a color
    - ItemForm validates title is required before submit
    - ItemForm assignee toggle switches between Me/Partner/None
    - ItemForm DateTimePicker allows selecting optional due date
    - All CSS files follow co-located pattern with dark theme variables
    - `npm run lint` passes with 0 errors
  </acceptance_criteria>
  <done>ItemRow displays items with checkbox, title, assignee badge, due date; ListForm creates/edits lists with name + color; ItemForm creates/edits items with title + assignee + due date; all components styled with co-located CSS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Forms | ListForm and ItemForm validate required fields before store calls |
| Store → Supabase | All writes go through todoStore which uses Supabase client with auth |
| Realtime → UI | Incoming events update store; UI re-renders from store state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | ItemForm assigned_to | low | accept | Values restricted to 'me'/'partner'/null by CHECK constraint in DB |
| T-08-05 | Information Disclosure | Assignee badge avatar | low | accept | Avatar URLs are Supabase storage signed URLs, not sensitive |
| T-08-06 | Tampering | Delete list confirmation | low | accept | Confirmation dialog prevents accidental deletion; RLS prevents cross-pair delete |
| T-08-07 | Denial of Service | Large list item count | low | accept | Completed section capped at max-height: 400px with overflow scroll (per UI-SPEC overflow rule) |

</threat_model>

<verification>
- Visual: Open AgendaPage, verify 4 tabs render. Click "Listas" tab
- Create list: Click FAB, enter name, pick color, submit. Verify list appears in grid
- Add item: Click list card, click FAB, enter title, set assignee, set due date, submit. Verify item appears
- Toggle: Click checkbox. Verify strikethrough and opacity change
- Sort: Add items with and without due dates. Verify due-dated items sort first
- Real-time: Open in second browser/incognito as partner. Verify items sync
- Delete: Long-press list card (or use edit mode), delete. Verify confirmation dialog and removal
- Collapsible: Complete items. Verify "Concluidos" section with count badge
- Lint: `npm run lint` passes
</verification>

<success_criteria>
1. AgendaPage renders 4 tabs with "Listas" as the 4th tab
2. ListsTab shows empty state, grid view, and item view correctly
3. ListCard displays name, color accent, item count
4. ItemRow shows checkbox, title, assignee badge, due date
5. ListForm creates/edits lists with name and color
6. ItemForm creates/edits items with title, assignee, due date
7. Collapsible "Concluidos" section works with count badge
8. Edit mode with bulk delete works
9. Delete confirmation dialogs appear for lists and items
10. Overdue items show red date text
11. `npm run lint` passes with 0 errors
</success_criteria>

<output>
Create `.planning/phases/08-shared-to-do-lists/08-02-SUMMARY.md` when done
</output>
