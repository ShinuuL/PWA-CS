---
phase: 08-shared-to-do-lists
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FRONTEND/src/stores/todoStore.js
  - FRONTEND/supabase/migrations/20260730_add_color_to_todo_lists.sql
  - FRONTEND/src/features/agenda/ColorPicker.jsx
  - FRONTEND/src/features/agenda/ColorPicker.css
  - FRONTEND/package.json
autonomous: true
requirements:
  - TODO-01
  - TODO-07
  - TODO-08

must_haves:
  truths:
    - "todoStore exposes lists, items, loading, error state and initializeTodos, createList, updateList, deleteList, createItem, updateItem, toggleItem, deleteItem, cleanup actions"
    - "initializeTodos subscribes to Supabase Realtime on todo_lists and todo_items tables filtered by pair_id"
    - "Realtime INSERT/UPDATE/DELETE events update store state for both lists and items"
    - "createList inserts into todo_lists with name, pair_id, created_by, and color (per D-07, D-19)"
    - "deleteList deletes from todo_lists and cascading delete removes associated todo_items"
    - "createItem inserts into todo_items with title, list_id, assigned_to, due_at, and created_by"
    - "toggleItem flips the completed boolean on a todo_item"
    - "deleteItem removes a single todo_item"
    - "cleanup removes the Realtime subscription channel and resets store state"
    - "ColorPicker renders a HexColorPicker from react-colorful with controlled value/onChange props"
    - "Migration adds color TEXT DEFAULT '#B87CFF' column to todo_lists table"
    - "react-colorful is installed in package.json"
  artifacts:
    - FRONTEND/src/stores/todoStore.js
    - FRONTEND/supabase/migrations/20260730_add_color_to_todo_lists.sql
    - FRONTEND/src/features/agenda/ColorPicker.jsx
    - FRONTEND/src/features/agenda/ColorPicker.css
  key_links:
    - "todoStore.initializeTodos subscribes to both todo_lists and todo_items Realtime channels"
    - "ColorPicker onChange propagates hex value to ListForm parent"
    - "Migration ALTER TABLE todo_lists ADD COLUMN color is idempotent-safe"
  prohibitions: []

---

<objective>
Create the data layer and color picker foundation for shared to-do lists. This plan installs react-colorful, adds a color column migration to todo_lists, builds the complete Zustand todoStore with Realtime subscriptions and full CRUD for lists and items, and creates the reusable ColorPicker component wrapping react-colorful's HexColorPicker.

Purpose: The todoStore is the single source of truth for all to-do data. Every UI component in Plan 2 depends on it. The color column migration enables list personalization (D-07, D-19, D-20). The ColorPicker is consumed by ListForm for list creation/editing.

Output: Working todoStore.js with Realtime sync, color column migration, ColorPicker component, react-colorful dependency installed.
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-shared-to-do-lists/08-CONTEXT.md
@.planning/phases/08-shared-to-do-lists/08-RESEARCH.md
@.planning/phases/08-shared-to-do-lists/08-UI-SPEC.md
@FRONTEND/src/stores/reminderStore.js
@FRONTEND/src/features/agenda/RemindersTab.jsx
@FRONTEND/supabase/migrations/20260728_create_todo_lists_and_items_tables.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: todoStore + Migration + Dependency</name>
  <files>FRONTEND/src/stores/todoStore.js, FRONTEND/supabase/migrations/20260730_add_color_to_todo_lists.sql, FRONTEND/package.json</files>
  <read_first>
    - FRONTEND/src/stores/reminderStore.js — exact pattern to mirror (initialize, Realtime subscription, optimistic updates, cleanup)
    - FRONTEND/src/stores/authStore.js — how to get current user ID for created_by and assignee resolution
    - FRONTEND/supabase/migrations/20260728_create_todo_lists_and_items_tables.sql — existing schema (id, pair_id, name, created_by, created_at for lists; id, list_id, title, completed, assigned_to, due_at, created_at for items)
    - FRONTEND/src/shared/lib/supabase.js — supabase client initialization
  </read_first>
  <behavior>
    - createList inserts a list with name, color, pair_id, created_by and returns the new list
    - deleteList removes a list and cascading deletes all its items
    - createItem inserts an item with title, list_id, assigned_to, due_at, created_by
    - toggleItem flips completed boolean
    - deleteItem removes a single item
    - initializeTodos subscribes to Realtime on todo_lists (filter: pair_id) and todo_items (filter: list_id IN (...))
    - Realtime INSERT/UPDATE/DELETE events update store state without duplicates
    - cleanup removes subscription and resets state
  </behavior>
  <action>
    1. Run `cd FRONTEND && npm install react-colorful` to add the dependency (per D-19, 3.1KB gzipped, zero deps, approved in Package Legitimacy Audit).

    2. Create migration `FRONTEND/supabase/migrations/20260730_add_color_to_todo_lists.sql`:
       - ALTER TABLE todo_lists ADD COLUMN color TEXT DEFAULT '#B87CFF'
       - COMMENT ON COLUMN todo_lists.color IS 'Hex color for list accent (D-07, D-19)'

    3. Create `FRONTEND/src/stores/todoStore.js` following the reminderStore.js pattern exactly:
       - State: lists[], items[], loading, error, pairId, subscription
       - initializeTodos(pairId): fetch lists from todo_lists (order by created_at asc), fetch items from todo_items where list_id IN (list IDs) (order by due_at asc nulls last, then created_at asc), clean up old subscription, subscribe to Realtime on both tables. For todo_lists filter: pair_id=eq.{pairId}. For todo_items filter: list_id=in.({listIds join comma}). If no lists exist, skip items subscription.
       - createList({ name, color }): optimistic insert with temp ID, Supabase insert, replace with server data on success, rollback on error. Store: pair_id, name, color, created_by, created_at.
       - updateList(listId, updates): optimistic update, Supabase update, rollback on error.
       - deleteList(listId): optimistic removal, Supabase delete (RLS cascade handles items), rollback on error.
       - createItem({ list_id, title, assigned_to, due_at }): optimistic insert with temp ID, Supabase insert, replace with server data, rollback on error. Store: list_id, title, completed=false, assigned_to, due_at, created_by.
       - updateItem(itemId, updates): optimistic update, Supabase update, rollback on error.
       - toggleItem(itemId): find item, flip completed, optimistic update, Supabase update, rollback on error.
       - deleteItem(itemId): optimistic removal, Supabase delete, rollback on error.
       - cleanup(): remove subscription channel, reset all state.
       - Realtime handlers: INSERT checks alreadyPresent before adding, UPDATE replaces matching item, DELETE filters out old.id. For items, also re-sort by due_at after each change.
       - Expose a getter or computed: getItemsForList(listId) that filters items by list_id and sorts by due date (items with dates first, then undated, then by created_at).
  </action>
  <verify>
    <automated>cd FRONTEND && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - When initializeTodos is called with a valid pairId, lists and items are loaded from Supabase
    - When createList is called, a new list appears in store.lists with the provided name and color
    - When deleteList is called, the list is removed from store.lists
    - When createItem is called, a new item appears in store.items with the provided title
    - When toggleItem is called, the item's completed field flips
    - When deleteItem is called, the item is removed from store.items
    - When cleanup is called, subscription is removed and state is reset
    - The Realtime subscription fires on INSERT/UPDATE/DELETE for both todo_lists and todo_items
    - package.json contains react-colorful as a dependency
    - The migration file adds color TEXT DEFAULT '#B87CFF' to todo_lists
  </acceptance_criteria>
  <done>todoStore.js exists with all CRUD actions and Realtime subscription; migration adds color column; react-colorful installed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: ColorPicker Component</name>
  <files>FRONTEND/src/features/agenda/ColorPicker.jsx, FRONTEND/src/features/agenda/ColorPicker.css</files>
  <read_first>
    - FRONTEND/src/stores/todoStore.js — just created, provides the store context
    - FRONTEND/src/features/agenda/RemindersTab.css — existing CSS variable patterns (--color-bg-card, --color-primary, --radius-lg)
    - docs/cosmic-v2.html — design reference for dark theme color system
  </read_first>
  <action>
    1. Create `FRONTEND/src/features/agenda/ColorPicker.jsx`:
       - Import HexColorPicker from 'react-colorful'
       - Export default function ColorPicker({ value, onChange })
       - Render HexColorPicker with controlled value and onChange
       - Add a hex input field below the picker for manual hex entry (validates 7-char hex format)
       - Wrap in a container div with className="color-picker"
       - Per D-19: full color wheel picker for maximum flexibility

    2. Create `FRONTEND/src/features/agenda/ColorPicker.css`:
       - Style the color-picker container with dark theme background (var(--color-bg-card))
       - Override react-colorful CSS variables: --rgb-background for container bg, --rgb-input for input bg
       - Style hex input: dark bg, light text, matching CoupleSpace input style
       - Ensure touch-friendly sizing (react-colorful handles this, but verify container min-height)
       - Use border-radius: var(--radius-lg) for container corners
  </action>
  <verify>
    <automated>cd FRONTEND && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - When ColorPicker renders, a hex color wheel is visible and interactive
    - When the user picks a color on the wheel, onChange is called with the hex value
    - When the user types a valid hex in the input, onChange is called with that value
    - The component accepts value and onChange as controlled props
    - The dark theme styling matches CoupleSpace's --color-bg-card and --color-primary variables
  </acceptance_criteria>
  <done>ColorPicker.jsx renders HexColorPicker from react-colorful with controlled props; ColorPicker.css applies dark theme styling; component is ready for ListForm integration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | All CRUD operations go through Supabase client with RLS enforcing pair_id membership |
| Realtime subscription → Store | Incoming Realtime events update store state; alreadyPresent checks prevent duplicates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | todoStore createList/createItem | medium | mitigate | RLS policies enforce pair_id membership; Supabase client handles auth token |
| T-08-02 | Information Disclosure | Realtime subscription | low | accept | Subscription filtered by pair_id; Supabase channel isolation prevents cross-pair leaks |
| T-08-03 | Tampering | ColorPicker hex input | low | accept | React auto-escapes; no dangerouslySetInnerHTML; hex validated on input |
| T-08-SC | Tampering | npm install react-colorful | low | mitigate | Package legitimacy audit passed (5.9M weekly downloads, 5+ years, zero deps, zero network calls in source) |

</threat_model>

<verification>
- Migration: Run the ALTER TABLE statement against Supabase (manual or CLI)
- Store: Verify all CRUD actions return expected state changes
- ColorPicker: Visual verification that hex wheel renders and responds to input
- Dependency: package.json lists react-colorful
- Lint: `npm run lint` passes with no errors
</verification>

<success_criteria>
1. todoStore.js exists with initializeTodos, createList, updateList, deleteList, createItem, updateItem, toggleItem, deleteItem, cleanup
2. Realtime subscription fires on todo_lists and todo_items INSERT/UPDATE/DELETE
3. Migration adds color column to todo_lists with default '#B87CFF'
4. react-colorful installed and ColorPicker component renders hex wheel
5. `npm run lint` passes
</success_criteria>

<output>
Create `.planning/phases/08-shared-to-do-lists/08-01-SUMMARY.md` when done
</output>
