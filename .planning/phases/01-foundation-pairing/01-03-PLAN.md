---
plan_id: 01-03
phase: 1
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - FRONTEND/src/features/profile/ProfilePage.jsx
  - FRONTEND/src/features/profile/PartnerProfile.jsx
  - FRONTEND/src/features/profile/AvatarUpload.jsx
  - FRONTEND/src/shared/components/AppShell.jsx
  - FRONTEND/src/shared/components/Drawer.jsx
  - FRONTEND/src/shared/components/Header.jsx
  - FRONTEND/src/features/settings/SettingsPage.jsx
  - FRONTEND/src/App.jsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
---

# Plan 01-03: Profile Management & App Shell

<objective>
Implement profile editing (display name, avatar upload), partner profile viewing, side drawer navigation, app shell layout, and settings page with unpair functionality.
</objective>

<read_first>
- FRONTEND/src/stores/authStore.js (profile state)
- FRONTEND/src/features/auth/useAuth.js (auth hook)
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-05 through D-12)
- .planning/phases/01-foundation-pairing/01-RESEARCH.md (Pattern 6: Avatar Upload, Side Drawer)
</read_first>

<tasks>

## Task 1: Create Profile Page

<action>
Create `FRONTEND/src/features/profile/ProfilePage.jsx` with edit form for display name. Uses `useAuth()` hook to get profile, calls `supabase.from('profiles').update()` on save. Create `FRONTEND/src/features/profile/profile.css` with CoupleSpace design tokens (var(--color-primary), var(--color-bg-input), var(--radius-md)).
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-10, D-12)
- docs/cosmic-v2.html (input styles, button styles)
</read_first>

<acceptance_criteria>
- [ ] ProfilePage shows current display name in input
- [ ] User can edit display name and save
- [ ] Display name required (save disabled when empty)
- [ ] Success/error messages displayed after save
- [ ] Page styled with CoupleSpace design tokens
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 2: Create Avatar Upload Component

<action>
Create `FRONTEND/src/features/profile/AvatarUpload.jsx`. Uses native `<input type="file" accept="image/*">` for camera/gallery picker. Validates file type (image/*) and size (<5MB). Uploads to Supabase Storage `avatars` bucket with path `{userId}/avatar.{ext}`. Updates profile `avatar_url` via `getPublicUrl()`. Shows avatar or initials placeholder, overlay on hover with "Change Photo".
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-11)
- .planning/phases/01-foundation-pairing/01-RESEARCH.md (Pattern 6: Avatar Upload)
</read_first>

<acceptance_criteria>
- [ ] AvatarUpload shows current avatar or initials placeholder
- [ ] File validation: type must be image, size < 5MB
- [ ] Upload to Supabase Storage avatars bucket with upsert
- [ ] Profile updated with new avatar_url after upload
- [ ] Preview shown during upload, error handling for failures
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 3: Create Partner Profile View

<action>
Create `FRONTEND/src/features/profile/PartnerProfile.jsx`. Fetches partner's profile via pairs table (find pair where user_one or user_two matches current user, get the other user's ID, then fetch their profile). Read-only view showing partner's avatar and display name. Handle case where no partner exists.
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-08, D-10)
- FRONTEND/src/stores/authStore.js (user state)
</read_first>

<acceptance_criteria>
- [ ] PartnerProfile fetches partner data via pairs table
- [ ] Shows partner avatar or initials placeholder
- [ ] Shows partner display name with "Your Partner" label
- [ ] Read-only view (no edit functionality)
- [ ] Loading state and no-partner state handled
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 4: Create Side Drawer Navigation

<action>
Create `FRONTEND/src/shared/components/Drawer.jsx` using Motion for React (`import { motion, AnimatePresence } from 'motion/react'`). Slides from left with spring animation. Nav items: Homepage, Chat (locked), Agenda (locked), Settings. Chat and Agenda locked when not paired (per D-09, show lock icon). Settings always accessible. Sign out button in footer. Overlay closes drawer on click. Create `drawer.css`.
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-05, D-08, D-09)
- .planning/phases/01-foundation-pairing/01-RESEARCH.md (Side Drawer pattern)
</read_first>

<acceptance_criteria>
- [ ] Drawer slides in from left with spring animation (Motion)
- [ ] Overlay closes drawer on click
- [ ] Nav items: Homepage, Chat, Agenda, Settings
- [ ] Chat and Agenda locked when not paired (per D-09)
- [ ] Settings always accessible (per D-09)
- [ ] Sign out button in footer, drawer header shows "CoupleSpace"
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 5: Create Header Component

<action>
Create `FRONTEND/src/shared/components/Header.jsx`. Shows hamburger menu icon (Menu from lucide-react), user's avatar (or initials placeholder), and display name. Menu button triggers drawer open via `onMenuClick` prop. Create `header.css`.
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-08)
- FRONTEND/src/stores/authStore.js (profile state)
</read_first>

<acceptance_criteria>
- [ ] Header shows hamburger menu icon, user avatar/initials, display name
- [ ] Menu button triggers drawer open via callback
- [ ] Header styled with CoupleSpace design tokens
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 6: Create App Shell Layout

<action>
Create `FRONTEND/src/shared/components/AppShell.jsx`. Wraps Header + Drawer + content area. Manages drawer open/close state. Create `appshell.css` with flex column layout (min-height: 100vh).
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-05, D-06)
</read_first>

<acceptance_criteria>
- [ ] AppShell renders Header, Drawer, and content area
- [ ] Drawer state managed (open/close via menu click)
- [ ] Content area fills remaining viewport height
- [ ] Mobile-first responsive layout
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 7: Create Settings Page

<action>
Create `FRONTEND/src/features/settings/SettingsPage.jsx`. Shows account info (email, display name). Unpair button with confirmation dialog — deletes pair record from database. Sign out button. Settings accessible without pairing (per D-09). Create `settings.css`.
</action>

<read_first>
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-04, D-09)
</read_first>

<acceptance_criteria>
- [ ] Settings page shows account info (email, display name)
- [ ] Unpair button with confirmation dialog before destructive action
- [ ] Unpair deletes pair record from database
- [ ] Sign out button calls signOut from auth store
- [ ] Settings accessible without pairing (per D-09)
</acceptance_criteria>

<type>implementation</type>
</task>

## Task 8: Update App Router with All Pages

<action>
Update `FRONTEND/src/App.jsx` to integrate all pages. Routes: /login, /auth/callback, /home, /chat, /agenda, /settings, /profile, /partner. ProtectedRoute wraps all auth-required pages. AppShell wraps page content. PairingGate wraps pages requiring pairing (/home, /chat, /agenda, /partner). /settings accessible without pairing. Default route redirects to /home.
</action>

<read_first>
- FRONTEND/src/App.jsx (current version from 01-01)
- .planning/phases/01-foundation-pairing/01-CONTEXT.md (D-06, D-07, D-09, D-10)
</read_first>

<acceptance_criteria>
- [ ] All routes use ProtectedRoute for authentication
- [ ] AppShell wraps protected pages
- [ ] PairingGate wraps pages requiring pairing
- [ ] /settings accessible without pairing
- [ ] /profile and /partner routes added
- [ ] Default route redirects to /home
</acceptance_criteria>

<type>implementation</type>
</task>

</tasks>

<verification>
## Verification Criteria

### Automated Checks
- [ ] `npm run build` completes without errors
- [ ] `npm run test:run` passes all tests
- [ ] No console errors in dev mode

### Manual Checks
- [ ] App shell renders with header and drawer
- [ ] Drawer opens/closes with smooth animation
- [ ] Nav items show locked state for Chat/Agenda when unpaired
- [ ] Settings accessible without pairing
- [ ] Profile page allows editing display name
- [ ] Avatar upload works with camera/gallery
- [ ] Partner profile shows partner info (when paired)
- [ ] Unpair functionality works with confirmation

### Security Checks
- [ ] RLS policies enforce pair isolation
- [ ] Avatar uploads restricted to own user folder
- [ ] Unpair only affects current user's pair
</verification>

<must_haves>
## Must Haves

### Truths (from specless probe — auto-resolved)
- "ProfilePage allows editing display name with validation"
- "AvatarUpload validates file type (image/*) and size (<5MB)"
- "AvatarUpload stores files in Supabase Storage avatars bucket"
- "PartnerProfile fetches partner data via pairs table"
- "Drawer navigation includes Homepage, Chat, Agenda, Settings"
- "Chat and Agenda locked when not paired (per D-09)"
- "Settings always accessible without pairing (per D-09)"
- "Header shows user avatar and display name"
- "AppShell provides consistent layout across pages"
- "Settings page includes unpair with confirmation dialog"

### Truths (from specless probe — unresolved, flagged assumptions)
- { statement: "Avatar upload works on mobile devices with camera access", verification: backstop }
- { statement: "Drawer animation performs smoothly on low-end devices", verification: backstop }

### Prohibitions (descriptor-less, flagged-unverified)
- { statement: "Do not allow inline profile editing (use separate page per D-10)", status: unverified, flagged: true }
- { statement: "Do not show unpaired users locked nav items without visual indication", status: unverified, flagged: true }
- { statement: "Do not allow avatar upload without file validation", status: unverified, flagged: true }
</must_haves>

<risks>
## Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Avatar upload fails on mobile | MEDIUM | Test with multiple devices, provide fallback UI |
| Drawer animation jank | LOW | Use CSS transforms, optimize Motion config |
| Partner profile fetch fails | LOW | Handle null state gracefully |
</risks>

<dependencies>
## Dependencies

- **Plan 01-01:** Project scaffold, Supabase client, auth store
- **Plan 01-02:** Auth flow, pairing system must exist
- **Supabase Storage:** Avatars bucket must be created (in 01-01 migration)
</dependencies>
