---
phase: 06-profile-enhancement-infrastructure-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - FRONTEND/src/shared/components/StatusDot.jsx
  - FRONTEND/src/shared/components/Header.jsx
  - FRONTEND/src/shared/components/header.css
  - FRONTEND/src/shared/components/Drawer.jsx
  - FRONTEND/src/shared/components/drawer.css
  - FRONTEND/src/features/chat/ChatView.jsx
  - FRONTEND/src/features/chat/chat.css
  - FRONTEND/src/features/profile/PartnerProfileModal.jsx
  - FRONTEND/src/features/profile/PartnerProfile.jsx
  - FRONTEND/src/features/profile/profile.css
autonomous: true
requirements:
  - INFRA-05
  - INFRA-06
  - PROF-04
  - PROF-05
  - PROF-06
  - PROF-07
must_haves:
  artifacts:
    - FRONTEND/src/shared/components/StatusDot.jsx
    - FRONTEND/src/features/profile/PartnerProfileModal.jsx
  truths:
    - StatusDot renders 8px circle with green (#22C55E) when online and gray (#8B8FA3) when offline
    - Header shows green/gray dot next to partner avatar when paired (per D-05)
    - ChatView shows status dot in chat header and "last seen X ago" when offline >1h (per D-06, D-07)
    - PartnerProfileModal shows bottom-sheet overlay with partner avatar + name + status dot + Message button (per D-25, D-26)
    - Drawer has partner profile button that opens PartnerProfileModal (per D-23)
    - Header partner avatar is tappable and opens PartnerProfileModal (per D-23)
    - "last seen" uses date-fns formatDistanceToNow relative time format (per D-06)
    - Display name changes appear in chat header and partner profile in real-time (per D-20)
    - profiles partner-read RLS policy already exists (INFRA-05 — verify, don't recreate)
---

<objective>
Build StatusDot component, integrate online presence across Header/ChatView/Drawer, and create PartnerProfileModal for viewing partner info.

Purpose: This completes the online status feature — partners can see each other's green/gray dot in real-time, "last seen" when offline, and view partner profile via a bottom-sheet modal. All UI integration points (header, drawer, chat) are wired up.

Output: StatusDot component, PartnerProfileModal, enhanced Header/Drawer/ChatView with presence
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-profile-enhancement-infrastructure-fixes/06-CONTEXT.md
@.planning/phases/06-profile-enhancement-infrastructure-fixes/06-RESEARCH.md
@.planning/phases/06-profile-enhancement-infrastructure-fixes/06-UI-SPEC.md

@FRONTEND/src/hooks/usePresence.js
@FRONTEND/src/shared/components/Header.jsx
@FRONTEND/src/shared/components/header.css
@FRONTEND/src/shared/components/Drawer.jsx
@FRONTEND/src/shared/components/drawer.css
@FRONTEND/src/features/chat/ChatView.jsx
@FRONTEND/src/features/chat/chat.css
@FRONTEND/src/features/profile/PartnerProfile.jsx
@FRONTEND/src/features/profile/profile.css
@FRONTEND/src/shared/lib/supabase.js
@FRONTEND/src/stores/authStore.js
@FRONTEND/src/features/auth/useAuth.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusDot component and integrate into Header and ChatView</name>
  <files>
    FRONTEND/src/shared/components/StatusDot.jsx
    FRONTEND/src/shared/components/Header.jsx
    FRONTEND/src/shared/components/header.css
    FRONTEND/src/features/chat/ChatView.jsx
    FRONTEND/src/features/chat/chat.css
  </files>
  <read_first>
    FRONTEND/src/shared/components/Header.jsx
    FRONTEND/src/shared/components/header.css
    FRONTEND/src/features/chat/ChatView.jsx
    FRONTEND/src/features/chat/chat.css
    FRONTEND/src/hooks/usePresence.js
    FRONTEND/src/shared/lib/supabase.js
    FRONTEND/src/stores/authStore.js
    FRONTEND/src/features/auth/useAuth.js
    FRONTEND/src/index.css
  </read_first>
  <action>
**Part A: Create StatusDot.jsx**

Create `FRONTEND/src/shared/components/StatusDot.jsx` — a small presentational component that renders an 8px colored circle.

Props: `isOnline` (boolean), `size` (number, default 8), `className` (string, optional)

Rendering:
- A `<span>` element with inline styles or className
- When `isOnline=true`: background `var(--color-online)` (#22C55E, per UI-SPEC)
- When `isOnline=false`: background `var(--color-offline)` (#8B8FA3, per UI-SPEC)
- Default size: 8px × 8px circle
- Use `display: inline-block`, `border-radius: 50%`, `flex-shrink: 0`
- Keep it minimal — this is a presentational dot, no logic

**Part B: Integrate into Header.jsx**

Enhance `FRONTEND/src/shared/components/Header.jsx` to show partner online status (per D-05):

1. Import `usePresence` from `../../hooks/usePresence`
2. Import `StatusDot` from `./StatusDot`
3. Get `user` from `useAuth()` (already imported)
4. Need partner's user ID — fetch it from the pair (the existing `fetchPartner` effect already resolves partnerId). Store partnerId in state alongside `partner` profile data.
5. Call `usePresence(pairId, partnerId)` where pairId comes from the pair fetch. Store pairId in state.
6. Render `<StatusDot isOnline={isOnline} />` next to the partner avatar inside `.header-avatar` wrapper. Position it absolutely at bottom-right of the avatar circle.
7. Make the partner avatar + name section tappable (per D-23) — add `onClick` that opens PartnerProfileModal (state: `showPartnerModal`). Pass `onClose` to close it.
8. Add `onClick={() => navigate('/partner')}` as a simple fallback until PartnerProfileModal is wired in Plan 3 Task 2. Alternatively, just make it clickable with a cursor pointer style.

Update `header.css`:
- Add `.header-avatar-wrapper` with `position: relative` for status dot positioning
- Add `.header-status-dot` with `position: absolute`, `bottom: -1px`, `right: -1px`, `border: 2px solid var(--color-bg-card)` for clean dot placement on avatar edge

**Part C: Integrate into ChatView.jsx**

Enhance `FRONTEND/src/features/chat/ChatView.jsx` to show partner status in chat header (per D-05, D-06, D-07):

1. Import `usePresence` from `../../hooks/usePresence`
2. Import `StatusDot` from `../../shared/components/StatusDot`
3. Import `formatDistanceToNow` from `date-fns`
4. Call `usePresence(pairId, ???)` — need the partner's user ID. The chat already has `pairId`. Fetch partner ID from the pair (similar to Header). Or, store partnerId in state when pair is loaded in the existing `checkPairStatus` effect.
5. In the chat header section (`.chat-header-info`), add `<StatusDot isOnline={isOnline} />` next to the partner name
6. Replace the static "Online" text with dynamic status:
   - If `isOnline`: show "Online" with green dot
   - If `!isOnline && lastSeen`: compute hours since lastSeen. If >1 hour, show `last seen ${formatDistanceToNow(lastSeen, { addSuffix: true })}` (per D-06, D-07). If <=1 hour, show nothing or "Offline"
7. The chat header currently shows `user?.user_metadata?.avatar_url` — this should actually show the PARTNER's avatar and name, not the current user's. Fix this to use the partner profile data fetched from the pair.

Update `chat.css`:
- Add `.chat-header-status` styles for the status text (smaller font, secondary color)
- Add status dot positioning within chat header
  </action>
  <verify>
    <automated>cd FRONTEND && grep -c "StatusDot" src/shared/components/StatusDot.jsx && grep -c "usePresence" src/shared/components/Header.jsx && grep -c "formatDistanceToNow" src/features/chat/ChatView.jsx</automated>
  </verify>
  <acceptance_criteria>
    - StatusDot.jsx exists with isOnline prop rendering colored circle
    - Header.jsx imports usePresence and renders StatusDot next to partner avatar
    - Header partner avatar is tappable with cursor pointer
    - ChatView.jsx imports usePresence and formatDistanceToNow
    - ChatView shows StatusDot in chat header
    - ChatView shows "last seen X ago" when offline >1 hour
    - ChatView shows "Online" when partner is online
    - header.css has status dot positioning styles
    - chat.css has status-related styles
  </acceptance_criteria>
  <done>
    StatusDot component renders green/gray circles. Header shows partner online status. ChatView shows dynamic status with "last seen" for offline partners.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PartnerProfileModal and integrate into Drawer</name>
  <files>
    FRONTEND/src/features/profile/PartnerProfileModal.jsx
    FRONTEND/src/shared/components/Drawer.jsx
    FRONTEND/src/shared/components/drawer.css
    FRONTEND/src/features/profile/PartnerProfile.jsx
    FRONTEND/src/features/profile/profile.css
  </files>
  <read_first>
    FRONTEND/src/features/profile/PartnerProfile.jsx
    FRONTEND/src/features/profile/profile.css
    FRONTEND/src/shared/components/Drawer.jsx
    FRONTEND/src/shared/components/drawer.css
    FRONTEND/src/shared/components/Header.jsx
    FRONTEND/src/shared/lib/supabase.js
    FRONTEND/src/stores/authStore.js
    FRONTEND/src/features/auth/useAuth.js
    FRONTEND/src/hooks/usePresence.js
    FRONTEND/src/shared/components/StatusDot.jsx
  </read_first>
  <action>
**Part A: Create PartnerProfileModal.jsx**

Create `FRONTEND/src/features/profile/PartnerProfileModal.jsx` — a bottom-sheet modal showing partner info (per D-23 through D-26).

Component API: `<PartnerProfileModal isOpen onClose />`

Props:
- `isOpen` (boolean) — controls modal visibility
- `onClose` (function) — closes the modal

Internal behavior:
1. Fetch partner profile from Supabase (same pattern as existing PartnerProfile.jsx):
   - Get current user from `useAuth()`
   - Query `pairs` table to find pair where user is one of the participants and `code_used = true`
   - Get partner ID (the other user in the pair)
   - Query `profiles` table for partner's profile
2. If no partner paired: show empty state — "No partner paired yet" heading, "Pair with your partner to see their profile" body (per Copywriting Contract)
3. If partner exists: show avatar (96px circle) + display name + StatusDot

Modal layout (per UI-SPEC Interaction Contracts):
- Bottom-sheet style: slides up from bottom using `motion/react`
- Background overlay with click-to-close
- Content: avatar circle (96px) + display name + StatusDot + "Message" button
- "Message" button: navigates to `/chat` and closes modal (per D-26)
- Close on overlay tap or swipe down gesture

Import paths:
- `import { motion, AnimatePresence } from 'motion/react'`
- `import { useNavigate } from 'react-router-dom'`
- `import { useAuth } from '../auth/useAuth'`
- `import { supabase } from '../../shared/lib/supabase'`
- `import { usePresence } from '../../hooks/usePresence'`
- `import StatusDot from '../../shared/components/StatusDot'`

Styling:
- Use co-located CSS (add modal styles to profile.css or create PartnerProfileModal.css)
- Bottom-sheet: position fixed, bottom 0, full width, max-height 70vh, rounded top corners
- Avatar: 96px circle with border
- "Message" button: primary style (var(--color-primary) background)

**Part B: Add partner profile button to Drawer**

Enhance `FRONTEND/src/shared/components/Drawer.jsx` (per D-23):

1. Add a new nav item at the top of the drawer (before NAV_ITEMS): a partner profile section showing:
   - Partner avatar (40px circle) + partner display name
   - Tappable → opens PartnerProfileModal
2. Fetch partner data using the same pattern from Header.jsx (query pairs + profiles)
3. Add state `showPartnerModal` (boolean)
4. Render PartnerProfileModal with isOpen/onClose props
5. If no partner: show "No partner paired yet" text instead of avatar

Update `drawer.css`:
- Add `.drawer-partner` styles: flex row with avatar + name, padding, tappable
- Add `.drawer-partner-avatar` styles: 40px circle

**Part C: Update PartnerProfile.jsx empty state**

Update `FRONTEND/src/features/profile/PartnerProfile.jsx` to match the Copywriting Contract:
- Empty state heading: "No partner paired yet" (currently "No partner paired yet." with period — remove period)
- Empty state body: "Pair with your partner to see their profile" (currently missing)
- This ensures consistency between the standalone page and the modal
  </action>
  <verify>
    <automated>cd FRONTEND && grep -c "PartnerProfileModal" src/features/profile/PartnerProfileModal.jsx && grep -c "PartnerProfileModal" src/shared/components/Drawer.jsx && grep -c "Message" src/features/profile/PartnerProfileModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - PartnerProfileModal.jsx exists and exports default function
    - PartnerProfileModal shows avatar + name + StatusDot + Message button
    - PartnerProfileModal uses motion/react for bottom-sheet animation
    - PartnerProfileModal navigates to /chat on Message button click
    - PartnerProfileModal shows empty state when no partner paired
    - Drawer has tappable partner profile section above nav items
    - Drawer renders PartnerProfileModal
    - PartnerProfile.jsx empty state text matches Copywriting Contract
    - profile.css has modal styles
    - drawer.css has partner section styles
  </acceptance_criteria>
  <done>
    PartnerProfileModal provides bottom-sheet view of partner profile with online status and Message button. Drawer has partner profile access point. All partner profile views are consistent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Client | Presence data flows peer-to-peer via Supabase Realtime channel |
| Client → Database | Partner profile fetched via RLS-protected query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | Presence spoofing | low | accept | Supabase channel auth ensures presence key = user ID; no custom auth needed |
| T-06-08 | Information Disclosure | PartnerProfileModal | low | accept | Only shows avatar + name + online status; RLS already restricts to paired users (INFRA-05) |
| T-06-09 | Denial of Service | usePresence cleanup | medium | mitigate | Cleanup function in useEffect removes channel on unmount (per Pitfall 3) |
</threat_model>

<verification>
- StatusDot component renders: `grep -c "isOnline" src/shared/components/StatusDot.jsx`
- Header has presence: `grep -c "usePresence" src/shared/components/Header.jsx`
- ChatView has presence: `grep -c "usePresence" src/features/chat/ChatView.jsx`
- PartnerProfileModal exists: `test -f src/features/profile/PartnerProfileModal.jsx`
- Drawer has partner section: `grep -c "PartnerProfileModal" src/shared/components/Drawer.jsx`
- Lint passes: `cd FRONTEND && npm run lint`
</verification>

<success_criteria>
- Green dot appears next to partner avatar in header when partner is online
- Gray dot appears when partner is offline
- "last seen X ago" shows in chat header when offline >1 hour
- Tapping partner avatar in header opens PartnerProfileModal
- Partner profile button in drawer opens PartnerProfileModal
- PartnerProfileModal shows avatar, name, status, and Message button
- Message button navigates to chat
- All presence channels clean up on unmount
</success_criteria>

<output>
Create `.planning/phases/06-profile-enhancement-infrastructure-fixes/06-03-SUMMARY.md` when done
</output>
