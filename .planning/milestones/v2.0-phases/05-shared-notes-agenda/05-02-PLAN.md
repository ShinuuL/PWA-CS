---
phase: 05-shared-notes-agenda
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - FRONTEND/src/features/agenda/EventsTab.jsx
  - FRONTEND/src/features/agenda/CalendarGrid.jsx
  - FRONTEND/src/features/agenda/EventForm.jsx
  - FRONTEND/src/features/agenda/EventRow.jsx
  - FRONTEND/src/features/agenda/NotesTab.jsx
  - FRONTEND/src/features/agenda/NoteEditor.jsx
  - FRONTEND/src/features/agenda/NoteCard.jsx
  - FRONTEND/src/features/agenda/agenda.css
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - AGND-01
  - AGND-02
  - AGND-03
  - AGND-04

must_haves:
  truths:
    - "User can create a shared note with title and body, and it appears in the notes list"
    - "Both partners can see and edit any note (true collaboration per D-07)"
    - "Notes are organized chronologically with created/edited dates"
    - "User can create an event with title, date, description, and category"
    - "Events display in a calendar grid at top and date-grouped list below"
    - "Calendar grid supports horizontal swipe to navigate months"
    - "User can set a reminder (1h, 1d, 1w) when creating an event"
    - "Both partners can see and create events"
    - "Empty states show illustration + CTA per D-04"
    - "Loading states show skeleton placeholders"
    - "Error states show retry CTA"
    - "All UI text is in pt-BR per UI-SPEC copywriting contract"
  artifacts:
    - path: "FRONTEND/src/features/agenda/EventsTab.jsx"
      provides: "Events tab with calendar grid + event list"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/CalendarGrid.jsx"
      provides: "Month-view calendar grid with swipe navigation"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/EventForm.jsx"
      provides: "Create/edit event form with category and reminder"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/EventRow.jsx"
      provides: "Single event display (cosmic-v2 style date block + info)"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/NotesTab.jsx"
      provides: "Notes list with empty/loading/error states"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/NoteEditor.jsx"
      provides: "Create/edit note form"
      exports: ["default"]
    - path: "FRONTEND/src/features/agenda/NoteCard.jsx"
      provides: "Single note display card"
      exports: ["default"]
  key_links:
    - from: "FRONTEND/src/features/agenda/EventsTab.jsx"
      to: "FRONTEND/src/stores/agendaStore.js"
      via: "EventsTab reads events from agendaStore"
      pattern: "useAgendaStore"
    - from: "FRONTEND/src/features/agenda/NotesTab.jsx"
      to: "FRONTEND/src/stores/notesStore.js"
      via: "NotesTab reads notes from notesStore"
      pattern: "useNotesStore"
    - from: "FRONTEND/src/features/agenda/CalendarGrid.jsx"
      to: "FRONTEND/src/features/agenda/EventsTab.jsx"
      via: "CalendarGrid passes selected date to EventsTab"
      pattern: "onDayClick"
    - from: "FRONTEND/src/features/agenda/EventForm.jsx"
      to: "FRONTEND/src/stores/agendaStore.js"
      via: "EventForm calls createEvent/updateEvent"
      pattern: "createEvent"
    - from: "FRONTEND/src/features/agenda/NoteEditor.jsx"
      to: "FRONTEND/src/stores/notesStore.js"
      via: "NoteEditor calls createNote/updateNote"
      pattern: "createNote"
---

<objective>
Build the complete UI for the Agenda page: Events tab with calendar grid and event management, Notes tab with note cards and editor, all with loading/error/empty states and pt-BR copy.

Purpose: This plan delivers the user-facing features that consume the data layer from Plan 01. Every requirement (NOTE-01 through AGND-04) becomes visible and interactive.

Output: Fully functional /agenda page with tabbed navigation, calendar grid, event CRUD, note CRUD, and all UI states (empty, loading, error, populated)
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shared-notes-agenda/05-CONTEXT.md
@.planning/phases/05-shared-notes-agenda/05-RESEARCH.md
@.planning/phases/05-shared-notes-agenda/05-UI-SPEC.md
@.planning/phases/05-shared-notes-agenda/05-01-SUMMARY.md
@FRONTEND/src/stores/notesStore.js
@FRONTEND/src/stores/agendaStore.js
@FRONTEND/src/features/agenda/AgendaPage.jsx
@FRONTEND/src/features/agenda/SegmentedTabs.jsx
@FRONTEND/src/features/dashboard/dashboard.css
@FRONTEND/src/features/album/album.css
@FRONTEND/docs/cosmic-v2.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EventsTab with CalendarGrid, EventRow, and EventForm</name>
  <files>FRONTEND/src/features/agenda/EventsTab.jsx, FRONTEND/src/features/agenda/CalendarGrid.jsx, FRONTEND/src/features/agenda/EventRow.jsx, FRONTEND/src/features/agenda/EventForm.jsx, FRONTEND/src/features/agenda/agenda.css</files>
  <behavior>
    - CalendarGrid renders a month grid with 7 columns (seg-dom), highlights today with accent background
    - CalendarGrid shows dot indicators on days that have events
    - CalendarGrid supports horizontal swipe to navigate months (motion drag="x")
    - EventRow displays date block (DD + month abbreviation), title, description preview, category pill
    - EventRow follows cosmic-v2.html event-row layout (lines 845-883)
    - EventsTab shows calendar grid at top, event list grouped by date below
    - EventsTab shows empty state with "Nenhum evento" heading + "Criar evento" CTA when no events
    - EventsTab shows skeleton loading state while events load
    - EventsTab shows error state with retry button on failure
    - EventForm has title (required), date (required), description (optional), category dropdown, reminder dropdown
    - EventForm category options: Noite de Date, Consulta, Aniversario, Viagem, Outro
    - EventForm reminder options: (nenhum), 1 hora antes, 1 dia antes, 1 semana antes
    - EventForm submit disabled until title and date are filled
  </behavior>
  <read_first>
    - FRONTEND/src/features/agenda/AgendaPage.jsx (how EventsTab is imported and rendered)
    - FRONTEND/src/stores/agendaStore.js (store API: events[], createEvent, deleteEvent, getEventsForDate)
    - FRONTEND/docs/cosmic-v2.html lines 845-883 (event-row design reference)
    - FRONTEND/src/features/dashboard/dashboard.css (CSS patterns: variables, spacing, animations)
    - FRONTEND/src/features/album/album.css (skeleton loading pattern)
    - .planning/phases/05-shared-notes-agenda/05-UI-SPEC.md (colors, typography, spacing, copywriting)
    - .planning/phases/05-shared-notes-agenda/05-RESEARCH.md (Pattern 3: calendar grid, Pattern 4: swipe gesture)
  </read_first>
  <action>
Create four components and update agenda.css for the Events tab.

**CalendarGrid.jsx:**
- Import startOfMonth, endOfMonth, startOfWeek, endOfWeek, eachDayOfInterval, format, isSameMonth, isSameDay, addMonths, subMonths, isToday from date-fns
- Import { ptBR } from date-fns/locale
- Import { motion } from motion/react
- Props: currentMonth, onMonthChange, events (array for dot indicators), onDayClick
- Generate calendar grid: eachDayOfInterval with start=startOfWeek(startOfMonth(currentMonth), weekStartsOn: 0), end=endOfWeek(endOfMonth(currentMonth), weekStartsOn: 0)
- Render weekday headers: seg, ter, qua, qui, sex, sab, dom
- Render 7-column CSS grid. Days in current month: full opacity. Days outside: opacity 0.3.
- Today cell: accent background (var(--color-primary))
- Days with events: small accent dot below day number
- Swipe gesture: motion.div with drag="x", dragConstraints left:0 right:0, onDragEnd checks offset threshold (>50px = prev month, <-50px = next month)
- Month label: format as "julho de 2026" using format(currentMonth, 'MMMM yyyy', { locale: ptBR })

**EventRow.jsx:**
- Props: event (object with title, description, event_date, category)
- Layout follows cosmic-v2.html event-row: left date block + right info
- Date block: day number (16px, weight 600) + month abbreviation (12px, uppercase, monospace)
- Event info: title (truncated with ellipsis), description preview (1 line max)
- Category pill: colored badge with category text
- Format event_date: day via format(date, 'd'), month via format(date, 'MMM', { locale: ptBR })

**EventForm.jsx:**
- Props: onSubmit, initialEvent (for edit mode, null for create), onCancel
- Controlled form: title input (required), event_date (date input, required), description textarea (optional), category select, reminder select
- Category options: Noite de Date, Consulta, Aniversario, Viagem, Outro
- Reminder options: null (Nenhum), 1h (1 hora antes), 1d (1 dia antes), 1w (1 semana antes)
- Submit: "Criar evento" (create) or "Salvar" (edit), disabled when title or date empty
- Inputs: var(--color-bg-input) background, var(--color-border) borders, focus var(--color-primary)

**EventsTab.jsx:**
- Import useAgendaStore for events, loading, error
- State: selectedDate (default today), showForm (boolean)
- Render CalendarGrid at top with events for dot indicators
- Event list grouped by date using reduce with format(date, 'yyyy-MM-dd') as key
- Empty state: "Nenhum evento" + "Adicione seu primeiro evento" + "Criar evento" CTA (D-04)
- Loading: skeleton placeholders (pulse animation per album.css)
- Error: "Algo deu errado - Tente novamente" + retry button
- FAB/CTA to open EventForm modal
- On submit: agendaStore.createEvent(formData), close form, react-hot-toast success
- Delete: agendaStore.deleteEvent(event.id) after confirmation

**agenda.css updates for Events tab:**
- Calendar grid: 7-column CSS grid with gap, day cells centered
- Event row: flex layout with date block left, info right (cosmic-v2 style)
- Category pill: small badge with accent-tinted background
- Skeleton: pulse animation matching album.css
- Modal overlay for EventForm: fixed positioning, backdrop
  </action>
  <verify>
    <automated>cd FRONTEND && grep -c "CalendarGrid" src/features/agenda/EventsTab.jsx && grep -c "EventRow" src/features/agenda/EventsTab.jsx && grep -c "EventForm" src/features/agenda/EventsTab.jsx && grep -c "drag" src/features/agenda/CalendarGrid.jsx && grep -c "Nenhum evento" src/features/agenda/EventsTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - EventsTab.jsx imports CalendarGrid, EventRow, EventForm, useAgendaStore
    - CalendarGrid uses date-fns functions (startOfMonth, eachDayOfInterval, etc.)
    - CalendarGrid has motion.div with drag="x" for swipe navigation
    - CalendarGrid renders 7-column grid with weekday headers (seg-dom)
    - EventRow renders date block + title + description + category pill
    - EventForm has title, date, description, category, reminder fields
    - EventForm category dropdown has 5 options
    - EventForm reminder dropdown has 4 options
    - EventsTab shows "Nenhum evento" empty state with "Criar evento" CTA
    - EventsTab groups events by date using date-fns
    - agenda.css contains calendar grid styles, event row styles, skeleton animation
    - grep -c "drag" src/features/agenda/CalendarGrid.jsx returns >= 1
    - grep -c "Nenhum evento" src/features/agenda/EventsTab.jsx returns >= 1
  </acceptance_criteria>
  <done>EventsTab displays calendar grid with swipe navigation, event list grouped by date, and EventForm for creating events. All states (empty, loading, error, populated) work correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Build NotesTab with NoteCard and NoteEditor, complete agenda.css</name>
  <files>FRONTEND/src/features/agenda/NotesTab.jsx, FRONTEND/src/features/agenda/NoteEditor.jsx, FRONTEND/src/features/agenda/NoteCard.jsx, FRONTEND/src/features/agenda/agenda.css</files>
  <behavior>
    - NotesTab shows notes list in reverse chronological order
    - NotesTab shows empty state with "Nenhuma nota ainda" heading + "Criar nota" CTA
    - NotesTab shows skeleton loading state while notes load
    - NotesTab shows error state with retry button on failure
    - NoteCard displays title (truncated), body preview (2 lines max), created date
    - NoteCard has edit and delete actions
    - NoteEditor has title (required) and body (optional, textarea) fields
    - NoteEditor submit disabled until title is filled
    - NoteEditor supports both create and edit modes
    - Delete confirmation shows "Excluir nota: Essa acao nao pode ser desfeita."
    - All text in pt-BR per UI-SPEC copywriting contract
  </behavior>
  <read_first>
    - FRONTEND/src/features/agenda/AgendaPage.jsx (how NotesTab is imported and rendered)
    - FRONTEND/src/stores/notesStore.js (store API: notes[], createNote, updateNote, deleteNote)
    - FRONTEND/src/features/agenda/agenda.css (existing styles from Task 1)
    - .planning/phases/05-shared-notes-agenda/05-UI-SPEC.md (copywriting, colors, typography, spacing)
    - .planning/phases/05-shared-notes-agenda/05-CONTEXT.md (D-05: plain text, D-06: title+body, D-07: true collaboration)
    - FRONTEND/src/features/dashboard/dashboard.css (CSS patterns)
  </read_first>
  <action>
Create three components and finalize agenda.css for the Notes tab.

**NoteCard.jsx:**
- Props: note (id, title, body, created_at, updated_at), onEdit, onDelete
- Display: title (16px, weight 600, truncated), body preview (max 2 lines with line-clamp, secondary color), date ("Criada em {date}" or "Editada em {date}" if updated)
- Format dates via date-fns format with pt-BR locale
- Actions: edit (pencil icon from lucide-react), delete (trash icon)
- Card: background var(--color-bg-card), border-radius var(--radius-md), padding var(--radius-md)

**NoteEditor.jsx:**
- Props: onSubmit, initialNote (null for create), onCancel
- Title input (required, placeholder "Titulo da nota"), body textarea (optional, placeholder "Escreva aqui... (opcional)")
- Submit: "Criar nota" (create) or "Salvar" (edit), disabled when title empty
- Body textarea: max-height constraint with vertical scroll
- Inputs: var(--color-bg-input) background, var(--color-border) borders, focus var(--color-primary)

**NotesTab.jsx:**
- Import useNotesStore for notes, loading, error
- State: editingNote (null or note object), showEditor (boolean)
- Render notes array (ordered by created_at DESC from store) as NoteCard list
- Empty state (D-04): "Nenhuma nota ainda" + "Crie sua primeira nota compartilhada" + "Criar nota" CTA
- Loading: skeleton placeholders (pulse animation)
- Error: "Algo deu errado - Tente novamente" + retry button
- FAB/CTA to open NoteEditor
- Create: notesStore.createNote(title, body), close editor
- Edit: populate NoteEditor, notesStore.updateNote(id, title, body)
- Delete: confirmation "Excluir nota: Essa acao nao pode ser desfeita.", then notesStore.deleteNote(note.id)
- Feedback via react-hot-toast

**agenda.css additions for Notes tab:**
- Note card styles (.note-card, .note-card__title, .note-card__body, .note-card__meta, .note-card__actions)
- Note editor styles (.note-editor, .note-editor__input, .note-editor__textarea)
- Notes tab states (.notes-tab__empty, .notes-tab__skeleton, .notes-tab__error)
- Line-clamp utility class
  </action>
  <verify>
    <automated>cd FRONTEND && grep -c "NoteCard" src/features/agenda/NotesTab.jsx && grep -c "NoteEditor" src/features/agenda/NotesTab.jsx && grep -c "Nenhuma nota ainda" src/features/agenda/NotesTab.jsx && grep -c "Criar nota" src/features/agenda/NoteEditor.jsx && grep -c "line-clamp" src/features/agenda/agenda.css</automated>
  </verify>
  <acceptance_criteria>
    - NotesTab.jsx imports NoteCard, NoteEditor, useNotesStore
    - NoteCard renders title (truncated), body preview (2 lines), created/edited date
    - NoteCard has edit and delete action buttons
    - NoteEditor has title input and body textarea
    - NoteEditor submit button text is "Criar nota" (create) or "Salvar" (edit)
    - NotesTab shows "Nenhuma nota ainda" empty state with "Criar nota" CTA
    - NotesTab shows skeleton loading state
    - NotesTab shows error state with retry
    - Delete confirmation text matches "Excluir nota: Essa acao nao pode ser desfeita."
    - agenda.css contains .note-card, .note-editor, line-clamp styles
    - grep -c "Nenhuma nota ainda" src/features/agenda/NotesTab.jsx returns >= 1
    - grep -c "Criar nota" src/features/agenda/NoteEditor.jsx returns >= 1
    - grep -c "line-clamp" src/features/agenda/agenda.css returns >= 1
  </acceptance_criteria>
  <done>NotesTab displays notes list with NoteCard and NoteEditor. All states (empty, loading, error, populated) work. All text in pt-BR. agenda.css complete with all component styles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI inputs -> Zustand store | Form inputs (note title/body, event details) cross from React components to store |
| Zustand store -> Supabase | Store methods send data to Supabase via JS client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | EventForm input | low | mitigate | Client-side validation (required fields), Supabase column constraints enforce NOT NULL |
| T-05-06 | Tampering | NoteEditor XSS | low | mitigate | React auto-escapes; D-05 plain text only (no markdown/rich text rendering) |
| T-05-07 | Denial of Service | CalendarGrid performance | low | accept | Month grid has fixed 42 cells max; no user-controlled iteration |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase |
</threat_model>

<verification>
1. CalendarGrid renders month grid with swipe navigation and event dot indicators
2. EventRow displays event with date block, title, description, category pill (cosmic-v2 style)
3. EventForm collects title, date, description, category, reminder
4. EventsTab shows calendar + grouped event list with all states
5. NoteCard displays note with title, body preview, date, edit/delete actions
6. NoteEditor collects title and body for create/edit
7. NotesTab shows note list with all states
8. All UI text matches UI-SPEC copywriting contract (pt-BR)
9. All CSS uses UI-SPEC design tokens
</verification>

<success_criteria>
- All 7 UI components (EventsTab, CalendarGrid, EventRow, EventForm, NotesTab, NoteEditor, NoteCard) are created
- Calendar grid works with swipe gesture for month navigation
- Event CRUD (create, read, delete) works end-to-end
- Note CRUD (create, read, update, delete) works end-to-end
- All empty/loading/error states render correctly
- All text is pt-BR per UI-SPEC copywriting contract
- All requirements (NOTE-01 through AGND-04) are verifiable
- **D-10 push notification deferral:** This plan implements the in-app reminder indicator only (reminder field stored and displayed in EventForm/EventRow). Browser push notifications (VAPID keys, Service Worker push handler, server-side subscription storage) are explicitly deferred to Phase 7 per RESEARCH.md resolution — the backend infrastructure does not exist yet and Notification Triggers API has poor Safari/Firefox support. The reminder field in agenda_events stores the user's preference ('1h', '1d', '1w') for future push integration.
</success_criteria>

<output>
Create .planning/phases/05-shared-notes-agenda/05-02-SUMMARY.md when done
</output>
