---
phase: 02-real-time-chat
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - FRONTEND/src/features/chat/ChatView.jsx
  - FRONTEND/src/features/chat/chat.css
  - FRONTEND/src/features/chat/ChatSettings.jsx
  - FRONTEND/src/features/chat/chatSettings.css
  - FRONTEND/src/stores/chatStore.js
  - FRONTEND/src/App.jsx
  - FRONTEND/public/sw.js
autonomous: false
requirements: [CHAT-07]
must_haves:
  truths:
    - Chat settings accessible from chat header icon per D-23
    - Settings include notification sounds, theme, font size, message preview toggle, read receipts toggle per D-24
    - Light, dark, and system default theme options work per D-25
    - Push notifications show message preview when app is in background per D-26
    - System default notification sounds used (no custom sounds) per D-27
    - No notification when app is open and in chat per D-28
    - Global notification settings only (no per-chat) per D-29
    - All interactive elements meet 44px minimum touch targets (CHAT-07)
    - Chat works smoothly on mobile devices (320px viewport minimum)
    - Font size preference affects message text size
  artifacts:
    - FRONTEND/src/features/chat/ChatSettings.jsx
    - FRONTEND/src/features/chat/chatSettings.css
    - FRONTEND/public/sw.js (updated with push notification handler)
    - FRONTEND/src/stores/chatStore.js (updated with settings state)
  key_links:
    - ChatSettings reads/writes settings from chatStore
    - Theme toggle updates CSS custom properties on :root
    - Push notification registration happens on chat mount
    - Service worker handles push events when app is background
---

<objective>
Add chat settings page with theme, font size, and notification preferences. Implement push notifications for messages when app is in background. Apply final mobile polish and ensure all interactive elements meet touch target requirements.

Purpose: Settings give users control over their chat experience. Push notifications ensure users never miss messages. Mobile polish ensures the chat feels native on phones.

Output: A settings page accessible from chat header, working push notifications, and a fully polished mobile chat experience.
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-chat/02-CONTEXT.md
@.planning/phases/02-real-time-chat/02-UI-SPEC.md
@FRONTEND/src/features/chat/ChatView.jsx
@FRONTEND/src/features/chat/chat.css
@FRONTEND/src/stores/chatStore.js
@FRONTEND/src/App.jsx
@FRONTEND/src/index.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Push Notification Setup & Service Worker</name>
  <what-built>
    Service worker updated with push notification handler. Push notification permission requested on chat mount. Supabase Edge Function skeleton created for sending push notifications when new messages are inserted.
  </what-built>
  <how-to-verify>
    1. Open the app in Chrome (or supported browser)
    2. Navigate to /chat
    3. Browser should prompt for notification permission
    4. Grant permission
    5. Send a message from another device/tab
    6. If app is in background, a notification should appear with message preview
    7. If app is in foreground and in chat, NO notification should appear (per D-28)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Chat Settings Page with Theme, Font Size, and Notification Preferences</name>
  <files>
    FRONTEND/src/features/chat/ChatSettings.jsx,
    FRONTEND/src/features/chat/chatSettings.css,
    FRONTEND/src/stores/chatStore.js,
    FRONTEND/src/features/chat/ChatView.jsx,
    FRONTEND/src/App.jsx
  </files>

  <read_first>
    - FRONTEND/src/features/chat/ChatView.jsx (current ChatHeader with settings icon)
    - FRONTEND/src/features/chat/chat.css (current styles)
    - FRONTEND/src/stores/chatStore.js (current store)
    - FRONTEND/src/features/settings/SettingsPage.jsx (existing settings page pattern)
    - FRONTEND/src/features/settings/settings.css (settings CSS pattern)
    - FRONTEND/src/App.jsx (routing pattern)
    - 02-CONTEXT.md (decisions D-23 through D-29)
    - 02-UI-SPEC.md (Copywriting Contract for settings)
  </read_first>

  <action>
Create ChatSettings page and update chatStore with settings state. This implements D-23 through D-29.

**chatStore.js updates:**

1. Add settings state:
   - `settings: { theme: 'system', fontSize: 16, notificationSounds: true, messagePreview: true, readReceipts: true }`
   - Load settings from localStorage on initialize
   - Save settings to localStorage on change

2. Add settings actions:
   - `updateSetting(key, value)` — Update a single setting and persist to localStorage
   - `applyTheme(theme)` — Apply theme to document: 'light', 'dark', or 'system' (prefers-color-scheme media query)

3. Add notification state:
   - `notificationPermission: 'default'` — Track permission status
   - `requestNotificationPermission()` — Request push notification permission
   - `isInChat: boolean` — Track whether user is currently viewing chat (for D-28)

4. Add push notification helper:
   - `showNotification(title, body)` — Show browser notification if permission granted AND not in chat (per D-28)

**ChatSettings.jsx:**

5. Create settings page component:
   - Route: /chat/settings (or modal overlay from chat)
   - Title: "Chat Settings"
   - Back button (lucide ArrowLeft) to return to chat

6. Settings sections (per D-24):

   a. **Notifications section:**
   - Toggle: "Notification Sounds" — on/off (per D-27: system default sounds)
   - Toggle: "Message Preview" — show/hide message content in notification (per D-26)
   - Note: "Notifications are global for all chats" (per D-29)
   - If permission not granted: show "Enable Notifications" button that requests permission

   b. **Appearance section:**
   - Theme selector: Light / Dark / System (per D-25)
   - Font Size: slider or selector (14px, 16px, 18px, 20px) — affects message text
   - Per D-12: Nunito font is default

   c. **Chat section:**
   - Toggle: "Read Receipts" — show/hide delivery indicators (per D-03)
   - Note: When off, delivery indicators are hidden for both users

7. Each toggle follows existing settings pattern from SettingsPage.jsx:
   - Label on left, toggle switch on right
   - Consistent with CoupleSpace design tokens

**ChatView.jsx updates:**

8. Update ChatHeader settings icon click:
   - Navigate to /chat/settings OR open settings as overlay
   - If using router: add /chat/settings route in App.jsx

9. Apply theme on mount:
   - Read theme from chatStore settings
   - Call chatStore.applyTheme(theme)
   - Listen for system theme changes if theme is 'system'

10. Apply font size:
    - Read fontSize from chatStore settings
    - Apply as CSS variable or inline style to message text elements

11. Read receipts visibility:
    - If readReceipts setting is false, hide DeliveryIndicator component

12. Track isInChat state:
    - On ChatView mount: set isInChat = true
    - On ChatView unmount: set isInChat = false
    - Notifications only shown when isInChat = false (per D-28)

**App.jsx updates:**

13. Add route for chat settings:
    - If using modal: no new route needed
    - If using page: add `<Route path="/chat/settings" element={<ProtectedRoute><AppShell><ChatSettings /></AppShell></ProtectedRoute>} />`

**chatSettings.css:**

14. Style the settings page:
    - Follow existing settings.css pattern
    - Toggle switches using CSS (no library needed)
    - Theme selector as radio buttons or segmented control
    - Font size slider or button group
    - All interactive elements 44px minimum touch target (CHAT-07)
  </action>

  <verify>
    <automated>cd FRONTEND && node -e "
const fs = require('fs');
const settings = fs.readFileSync('src/features/chat/ChatSettings.jsx', 'utf8');
const store = fs.readFileSync('src/stores/chatStore.js', 'utf8');
const settingsChecks = ['ChatSettings', 'theme', 'fontSize', 'notificationSounds', 'readReceipts', 'notificationSounds', 'messagePreview'];
const storeChecks = ['settings', 'updateSetting', 'applyTheme', 'notificationPermission', 'requestNotificationPermission', 'isInChat'];
settingsChecks.forEach(c => { if (!settings.includes(c)) { console.error('SETTINGS MISSING: ' + c); process.exit(1); } });
storeChecks.forEach(c => { if (!store.includes(c)) { console.error('STORE MISSING: ' + c); process.exit(1); } });
console.log('All settings elements present');
"</automated>
  </verify>

  <acceptance_criteria>
    - ChatSettings.jsx exists with notification, appearance, and chat sections
    - Theme selector offers Light, Dark, System options per D-25
    - Font size selector affects message text size
    - Notification toggles control sound and preview per D-24, D-26, D-27
    - Read receipts toggle controls delivery indicator visibility per D-03
    - Settings persist in localStorage
    - Chat header settings icon navigates to settings
    - All interactive elements meet 44px touch targets (CHAT-07)
    - `npm run build` succeeds
  </acceptance_criteria>

  <done>
    Chat settings page with theme, font size, notification, and read receipt preferences. Settings persist across sessions and apply immediately.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mobile Polish, Touch Targets, and Final Verification</name>
  <files>
    FRONTEND/src/features/chat/ChatView.jsx,
    FRONTEND/src/features/chat/chat.css,
    FRONTEND/src/App.jsx
  </files>

  <read_first>
    - FRONTEND/src/features/chat/ChatView.jsx (full current state after all previous tasks)
    - FRONTEND/src/features/chat/chat.css (full current styles)
    - FRONTEND/src/App.jsx (current routing)
    - 02-UI-SPEC.md (Spacing Scale, Typography, Layout Contract — final pass)
    - 02-CONTEXT.md (all decisions — final verification)
  </read_first>

  <action>
Final mobile polish pass. Review all chat components against the UI-SPEC and fix any issues. This is the quality gate for CHAT-07 (mobile optimization).

**Touch Target Audit (CHAT-07):**
1. Verify all interactive elements have minimum 44px touch targets:
   - Send button in MessageInput: 44px circle ✓
   - Context menu items: min 44px height ✓
   - Reaction picker emojis: 44px min-width/height ✓
   - Reaction chips: 44px min-height when tappable ✓
   - Back button in ChatHeader: 44px ✓
   - Settings icon in ChatHeader: 44px ✓
   - NewMessagesButton: 44px height ✓
   - QuotePreview cancel (X) button: 44px ✓
   - Delete confirm buttons: 44px height ✓
   - Any missed interactive elements: add min-height/width 44px

**Mobile Layout Audit:**
2. Verify chat layout works at 320px viewport width:
   - Message bubbles max-width 80% (not overflowing)
   - Input area doesn't overlap keyboard (use visual viewport API or padding-bottom)
   - Header doesn't overflow (partner name truncates with ellipsis)
   - Context menu doesn't overflow screen edges
   - Reaction picker fits within viewport width
   - Date separators don't overflow

3. Fix any layout issues found:
   - Add `max-width: 100%` and `word-break: break-word` to message content
   - Ensure flex layouts don't cause horizontal overflow
   - Test with long messages, long partner names, many reactions

**Scroll Behavior Audit:**
4. Verify auto-scroll rules per D-11:
   - If user is at bottom (within 100px): auto-scroll to new message
   - If user has scrolled up: show "New messages" floating button
   - Clicking "New messages" scrolls to bottom smoothly
   - Scroll position preserved when loading more history (if implemented)

**Typography Audit:**
5. Verify all text sizes per D-13 and UI-SPEC:
   - Message text: 16px / weight 400 / line-height 1.5
   - Timestamps: 14px / weight 400 / line-height 1.4
   - Reaction counts: 12px / weight 600
   - Chat header name: 16px / weight 600
   - Date separators: 12px / weight 600 / uppercase / letter-spacing 1px
   - Quote preview: 14px / weight 400 / line-height 1.4 / max 3 lines
   - Deleted placeholder: 14px / weight 400 / italic

**Color Audit:**
6. Verify all colors match UI-SPEC:
   - Sent bubble: purple gradient (--chat-bubble-sent)
   - Received bubble: subtle white (--chat-bubble-received)
   - Timestamps: --chat-timestamp (#5E5B75)
   - Date separators: --chat-date-separator (#8B8FA3)
   - Reaction chips: --chat-reaction-bg, --chat-reaction-border
   - Deleted text: --chat-deleted-text (#5E5B75)
   - Destructive (delete): #EF4444

**Animation Audit:**
7. Verify all animations match UI-SPEC Animation Contract:
   - New message: slide up + fade in, 200ms ease-out
   - Reaction add: scale pop-in, 200ms spring
   - Reaction remove: scale down, 150ms ease-in
   - Context menu: fade in + translateY, 150ms ease-out
   - Delete dialog: backdrop fade + modal scale, 200ms ease-out
   - New messages button: slide up, 200ms ease-out
   - Typing dots: sequential bounce, 600ms loop
   - Date separator: fade in, 300ms ease-in

**Edge Cases:**
8. Handle edge cases:
   - Very long messages: wrap properly, no horizontal scroll
   - Empty message prevention: input validation (no empty/whitespace-only sends)
   - Rapid message sending: debounced or queued
   - Partner offline: messages queue and sync (D-02)
   - Multiple reactions from same user: toggle behavior works
   - Message with both reply and reactions: layout doesn't break

**Final Build Verification:**
9. Run `npm run build` and verify no errors
10. Run `npm run test:run` and verify tests pass
  </action>

  <verify>
    <automated>cd FRONTEND && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - All interactive elements have minimum 44px touch targets (CHAT-07)
    - Chat layout works at 320px viewport width without overflow
    - Message text wraps properly with word-break
    - All typography sizes match UI-SPEC exactly
    - All colors match UI-SPEC token values
    - All animations match UI-SPEC Animation Contract
    - Auto-scroll behavior works correctly per D-11
    - Empty message prevention works (no whitespace-only sends)
    - `npm run build` succeeds with no errors
    - `npm run test:run` passes
    - No console errors in browser
  </acceptance_criteria>

  <done>
    Chat is fully polished for mobile with correct touch targets, typography, colors, animations, and layout. All edge cases handled. Build and tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → browser notifications | Push notifications cross from app to OS notification system |
| client → localStorage | Settings stored locally; no sensitive data in settings |
| client → service worker | Service worker handles push events in background |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Info Disclosure | push notifications | medium | mitigate | Message preview toggle (D-26) lets user hide content; notification only shows when not in chat (D-28) |
| T-02-11 | Tampering | localStorage settings | low | accept | Settings are user preferences only; no security impact if tampered |
| T-02-12 | Denial of Service | service worker | low | accept | Service worker is standard web API; browser manages lifecycle |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run build` passes
- `npm run test:run` passes
- Push notification permission request works
- Settings page accessible from chat header
- Theme switching works (light/dark/system)
- Font size preference applies to messages
- All touch targets >= 44px
</verification>

<success_criteria>
- Chat settings page with theme, font size, notification, and read receipt preferences
- Push notifications work when app is in background
- No notifications when app is open in chat
- All interactive elements meet 44px touch targets
- Chat works smoothly on mobile devices (320px+)
- All typography, colors, and animations match UI-SPEC
- Build and tests pass
- No regressions from Plans 02-01 and 02-02
</success_criteria>

<output>
Create `.planning/phases/02-real-time-chat/02-03-SUMMARY.md` when done
</output>
