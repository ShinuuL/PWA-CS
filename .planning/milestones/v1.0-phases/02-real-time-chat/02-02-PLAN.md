---
phase: 02-real-time-chat
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - FRONTEND/src/features/chat/ChatView.jsx
  - FRONTEND/src/features/chat/chat.css
  - FRONTEND/src/stores/chatStore.js
autonomous: true
requirements: [CHAT-05, CHAT-06]
must_haves:
  truths:
    - User can long-press a message to open context menu with Reply and Delete options
    - User can swipe right on a message to initiate reply (mobile)
    - Reply mode shows quoted message preview above input with X to cancel
    - Sent reply message displays inline quote with original message text (max 3 lines with ellipsis)
    - Quote preview has left border with light background per D-17
    - User can long-press to open reaction picker with 8 emoji options per D-19
    - Tapping an emoji adds reaction; tapping again removes own reaction (toggle)
    - Reactions display below message bubble with emoji + count per D-20, D-22
    - Own reactions are highlighted with primary color border per UI-SPEC
    - Reaction pop-in animation works with scale spring per D-21
    - User can delete messages for self or for everyone per D-30
    - Delete confirmation dialog shows before deletion per D-33
    - Deleted messages show "This message was deleted" placeholder per D-32
    - Typing indicator shows animated dots when partner is composing per D-04
    - Typing indicator dismisses 3 seconds after partner stops typing
  artifacts:
    - FRONTEND/src/features/chat/ChatView.jsx (updated with QuotePreview, ContextMenu, ReactionPicker, ReactionChip, DeleteConfirmDialog, TypingIndicator)
    - FRONTEND/src/features/chat/chat.css (updated with new component styles)
    - FRONTEND/src/stores/chatStore.js (updated with reply state, delete confirmation)
  key_links:
    - ContextMenu triggers chatStore.deleteMessage and reply mode
    - ReactionPicker calls chatStore.addReaction
    - TypingIndicator reads chatStore.partnerTyping
    - QuotePreview displays quoted message context above MessageInput
---

<objective>
Add interactive chat features: reply-to-message with inline quoting, emoji reactions with picker, message deletion with confirmation dialog, and typing indicators. These features transform basic messaging into a rich, WhatsApp-style chat experience.

Purpose: Replies and reactions are core interaction patterns that make the chat feel alive and contextual. Typing indicators provide real-time feedback that the partner is engaged.

Output: A fully interactive chat where users can reply to specific messages, react with emojis, delete messages, and see typing indicators.
</objective>

<execution_context>
@C:/Users/kinga/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/kinga/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-chat/02-CONTEXT.md
@.planning/phases/02-real-time-chat/02-UI-SPEC.md
@FRONTEND/src/features/chat/ChatView.jsx
@FRONTEND/src/features/chat/chat.css
@FRONTEND/src/stores/chatStore.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reply System — QuotePreview, ContextMenu, and Reply Mode</name>
  <files>
    FRONTEND/src/features/chat/ChatView.jsx,
    FRONTEND/src/features/chat/chat.css,
    FRONTEND/src/stores/chatStore.js
  </files>

  <read_first>
    - FRONTEND/src/features/chat/ChatView.jsx (current state from Plan 02-01 — MessageBubble, MessageInput, layout)
    - FRONTEND/src/features/chat/chat.css (current styles)
    - FRONTEND/src/stores/chatStore.js (current store — state shape and actions)
    - 02-CONTEXT.md (decisions D-14 through D-17, D-30 through D-33)
    - 02-UI-SPEC.md (Interaction Contract: Long-Press, Swipe to Reply, Context Menu, Delete Confirmation)
  </read_first>

  <action>
Update ChatView.jsx, chat.css, and chatStore.js to add reply-to-message functionality and message deletion. This task implements D-14 through D-17 (reply/quote UX) and D-30 through D-33 (message deletion).

**chatStore.js updates:**

1. Add new state fields:
   - `replyTo: null` — the message object being replied to (null when not in reply mode)
   - `showDeleteConfirm: false` — whether delete confirmation dialog is open
   - `deleteTarget: null` — the message targeted for deletion
   - `deleteForEveryone: false` — whether delete is for everyone or just self

2. Add new actions:
   - `setReplyTo(message)` — Set the reply target message
   - `cancelReply()` — Clear reply state
   - `openDeleteConfirm(message)` — Show delete confirmation dialog
   - `closeDeleteConfirm()` — Hide delete confirmation
   - `confirmDelete()` — Call deleteMessage with the stored forEveryone flag

3. Update `sendMessage(content)` to include `reply_to: replyTo?.id` when replying, then call `cancelReply()` after send.

**ChatView.jsx updates:**

4. **QuotePreview component** — Per D-14, D-16, D-17:
   - Rendered above MessageInput when replyTo is set
   - Shows: partner's display name + first 2-3 lines of original message with CSS line-clamp: 3 and ellipsis (D-16)
   - Left border: 3px solid --chat-reply-border (D-17)
   - Background: --chat-reply-bg
   - X button (lucide X) to cancel reply — positioned top-right of quote
   - Animated in with motion (slide up + fade in)

5. **ContextMenu component** — Per D-18, D-31:
   - Triggered by long-press (500ms hold) on any MessageBubble
   - Also triggered by right-click on desktop
   - Shows above the pressed message, centered horizontally
   - Two items: Reply (lucide Reply icon + "Reply" text), Delete (lucide Trash2 icon + "Delete" text in red)
   - Each item minimum 44px touch target (CHAT-07)
   - Background: --color-bg-card, border: 1px solid --color-border, border-radius 12px
   - Dismiss on tap outside (click overlay)
   - Animation: fade in + translateY(-4px to 0), 150ms ease-out
   - On Reply tap: call chatStore.setReplyTo(message), dismiss menu
   - On Delete tap: call chatStore.openDeleteConfirm(message), dismiss menu

6. **DeleteConfirmDialog component** — Per D-33:
   - Modal overlay with backdrop
   - Two variants based on whether user is message sender:
     - Delete for everyone (sender only): "Delete message?" / "This will remove the message for everyone. This cannot be undone."
     - Delete for self: "Delete message?" / "This will remove the message from your chat only."
   - Two buttons: "Delete" (red, destructive) and "Cancel"
   - Animation: backdrop fade in + modal scale 0.95 to 1.0, 200ms ease-out
   - On confirm: call chatStore.confirmDelete()
   - On cancel: call chatStore.closeDeleteConfirm()

7. **MessageBubble updates:**
   - Add onContextMenu handler for desktop right-click
   - Add long-press detection (onPointerDown/onPointerUp with 500ms timer)
   - If message has reply_to: render inline QuotePreview showing the original message above the current message text
   - QuotePreview inside bubble: left border, --chat-reply-bg background, truncated original text
   - If message.deleted_for_everyone or message.deleted: show "This message was deleted" italic text (per D-32)

8. **MessageInput updates:**
   - When replyTo is set: show QuotePreview above the input area
   - The quote shows the message being replied to with partner name and truncated text
   - X button to cancel reply
   - Send button calls sendMessage which includes reply_to

9. **Swipe to Reply (mobile)** — Per D-15:
   - Add touch gesture handling on MessageBubble
   - Track horizontal drag distance on pointer events
   - If drag right > 80px: trigger reply mode (setReplyTo)
   - Animate message sliding right during drag, spring back on release
   - Use motion library for spring animation (stiffness 300, damping 30)

10. **Long-press haptic feedback** — Per D-18:
    - On long-press trigger, call `navigator.vibrate(50)` if supported
    - This is a progressive enhancement — no-op on unsupported devices

**CSS additions to chat.css:**
- `.quote-preview` — left border, background, line-clamp, padding
- `.context-menu` — absolute positioning, background, border, shadow, border-radius
- `.context-menu-item` — flex layout, min-height 44px, icon + text
- `.context-menu-item--delete` — red text color
- `.delete-confirm-dialog` — modal overlay, centered card, backdrop
- `.delete-confirm-dialog__button--delete` — red background
- `.swipe-indicator` — visual feedback during swipe gesture
  </action>

  <verify>
    <automated>cd FRONTEND && node -e "
const fs = require('fs');
const store = fs.readFileSync('src/stores/chatStore.js', 'utf8');
const chat = fs.readFileSync('src/features/chat/ChatView.jsx', 'utf8');
const css = fs.readFileSync('src/features/chat/chat.css', 'utf8');
const storeChecks = ['replyTo', 'setReplyTo', 'cancelReply', 'openDeleteConfirm', 'closeDeleteConfirm', 'confirmDelete', 'deleteForEveryone'];
const chatChecks = ['QuotePreview', 'ContextMenu', 'DeleteConfirmDialog', 'onContextMenu', 'long-press', 'line-clamp'];
const cssChecks = ['quote-preview', 'context-menu', 'delete-confirm'];
storeChecks.forEach(c => { if (!store.includes(c)) { console.error('STORE MISSING: ' + c); process.exit(1); } });
chatChecks.forEach(c => { if (!chat.includes(c)) { console.error('CHAT MISSING: ' + c); process.exit(1); } });
cssChecks.forEach(c => { if (!css.includes(c)) { console.error('CSS MISSING: ' + c); process.exit(1); } });
console.log('All reply/delete elements present');
"</automated>
  </verify>

  <acceptance_criteria>
    - Long-press on message shows ContextMenu with Reply and Delete options (D-18, D-31)
    - Context menu items have minimum 44px touch targets (CHAT-07)
    - Swipe right on message initiates reply mode (D-15)
    - Reply mode shows QuotePreview above MessageInput with original message text (D-14, D-16)
    - QuotePreview has left border with light background (D-17)
    - X button in QuotePreview cancels reply mode
    - Sent reply message displays inline quote in MessageBubble
    - Delete opens confirmation dialog with correct copy per D-33
    - Confirming delete removes message and shows placeholder (D-30, D-32)
    - All animations use motion library with correct durations from UI-SPEC
    - `npm run build` succeeds
  </acceptance_criteria>

  <done>
    Users can long-press or swipe to reply to messages, see quoted context, and delete messages with confirmation. Context menu, delete dialog, and quote previews are fully styled and animated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emoji Reactions, Typing Indicator, and Reaction Animations</name>
  <files>
    FRONTEND/src/features/chat/ChatView.jsx,
    FRONTEND/src/features/chat/chat.css,
    FRONTEND/src/stores/chatStore.js
  </files>

  <read_first>
    - FRONTEND/src/features/chat/ChatView.jsx (current state after Task 1 — MessageBubble with context menu, QuotePreview)
    - FRONTEND/src/features/chat/chat.css (current styles after Task 1)
    - FRONTEND/src/stores/chatStore.js (current store after Task 1 — addReaction already exists)
    - 02-CONTEXT.md (decisions D-04, D-18 through D-22)
    - 02-UI-SPEC.md (Reaction Picker, Reaction Chips, Typing Indicator, Animation Contract)
  </read_first>

  <action>
Update ChatView.jsx, chat.css, and chatStore.js to add emoji reactions with picker, reaction chips display, and typing indicator. This implements D-04 (typing), D-18 through D-22 (reactions).

**ChatView.jsx updates:**

1. **ReactionPicker component** — Per D-18, D-19, D-21:
   - Triggered from ContextMenu (add "React" option) OR from long-press directly
   - Shows 8 emojis in a horizontal row: ❤️ 😂 👍 👎 😢 🔥 😍 🎉 (D-19)
   - Positioned above the message bubble, centered horizontally
   - Each emoji is a button with minimum 44px touch target (CHAT-07)
   - Tap emoji: call chatStore.addReaction(messageId, emoji) — toggles on/off (D-18)
   - Tap same emoji again: removes own reaction (toggle behavior)
   - Animation: scale from 0.8 to 1.0 + fade in, 200ms spring (stiffness 400, damping 25) per UI-SPEC
   - Dismiss: tap outside, or after selecting an emoji
   - Show which emoji user already reacted with (subtle highlight/border)

2. **ReactionChip component** — Per D-20, D-21, D-22:
   - Rendered below MessageBubble when message has reactions
   - Horizontal flex layout, gap 4px
   - Each chip: rounded pill, emoji + count (e.g., "❤️ 3") per D-22
   - Background: --chat-reaction-bg, border: 1px solid --chat-reaction-border
   - Own reaction highlight: border becomes --color-primary with brighter background per UI-SPEC
   - Animation: scale from 0 to 1 with spring physics per D-21 (stiffness 300, damping 20)
   - Right-aligned for sent messages, left-aligned for received per UI-SPEC
   - Click on own reaction: remove reaction (toggle off)

3. **TypingIndicator component** — Per D-04:
   - Shown when chatStore.partnerTyping is true
   - Three animated dots in a small bubble, left-aligned (like received message)
   - Dots animate sequentially: scale 1.0 → 1.3 → 1.0, staggered 150ms per dot, 600ms loop per UI-SPEC
   - Uses motion library for dot animation
   - Dismissed automatically 3 seconds after partner stops typing (handled in chatStore.setTyping)

4. **MessageBubble updates for reactions:**
   - After message content and timestamp/delivery indicator, render ReactionChip row if message has reactions
   - Pass message.reactions array to ReactionChip component
   - Reactions array comes from the Realtime subscription (already in store from Plan 02-01)

5. **ContextMenu updates:**
   - Add third option: "React" (lucide SmilePlus icon)
   - On React tap: open ReactionPicker for that message
   - Reorder: Reply, React, Delete (top to bottom)

6. **MessageList updates:**
   - After MessageBubble + ReactionChip row, check if next message is from same sender and within 5 min — if not, show TimeDivider
   - TypingIndicator rendered at bottom of message list when partnerTyping is true

**chatStore.js updates:**

7. Verify existing `addReaction` and `removeReaction` actions handle the toggle logic correctly:
   - Check if user already has this reaction (filter reactions by user_id and emoji)
   - If yes: DELETE the reaction
   - If no: INSERT the reaction
   - The Realtime subscription on reactions table (from Plan 02-01) handles updating local state

8. Verify `setTyping` action:
   - UPSERT into typing_status with is_typing flag
   - If setting to true, start a 3-second timeout to auto-set to false
   - If called again before timeout, clear previous timeout and start new one (debounce)
   - Partner typing detected via Realtime subscription on typing_status table

**CSS additions to chat.css:**
- `.reaction-picker` — horizontal row, centered, background, border-radius, shadow, padding
- `.reaction-picker__emoji` — button reset, font-size 24px, min-width/height 44px, hover/active states
- `.reaction-chip` — pill shape, flex inline, gap, font-size 12px, background, border
- `.reaction-chip--own` — primary border, brighter background
- `.typing-indicator` — small bubble, left-aligned, flex row, gap between dots
- `.typing-indicator__dot` — circle, animated scale
- `@keyframes typing-bounce` — sequential dot bounce animation
- `.message-bubble--deleted` — italic, muted color, no bubble styling
  </action>

  <verify>
    <automated>cd FRONTEND && node -e "
const fs = require('fs');
const chat = fs.readFileSync('src/features/chat/ChatView.jsx', 'utf8');
const css = fs.readFileSync('src/features/chat/chat.css', 'utf8');
const store = fs.readFileSync('src/stores/chatStore.js', 'utf8');
const chatChecks = ['ReactionPicker', 'ReactionChip', 'TypingIndicator', 'emoji', 'addReaction', 'partnerTyping'];
const cssChecks = ['reaction-picker', 'reaction-chip', 'typing-indicator', 'typing-bounce'];
const storeChecks = ['addReaction', 'removeReaction', 'setTyping', 'partnerTyping'];
chatChecks.forEach(c => { if (!chat.includes(c)) { console.error('CHAT MISSING: ' + c); process.exit(1); } });
cssChecks.forEach(c => { if (!css.includes(c)) { console.error('CSS MISSING: ' + c); process.exit(1); } });
storeChecks.forEach(c => { if (!store.includes(c)) { console.error('STORE MISSING: ' + c); process.exit(1); } });
console.log('All reaction/typing elements present');
"</automated>
  </verify>

  <acceptance_criteria>
    - Long-press shows ReactionPicker with 8 emojis per D-19
    - Tapping emoji adds reaction; tapping again removes it (toggle)
    - Reactions display below message bubble with emoji + count per D-20, D-22
    - Own reactions highlighted with primary border per UI-SPEC
    - Reaction pop-in animation uses spring physics per D-21
    - TypingIndicator shows animated dots when partner types per D-04
    - Typing indicator auto-hides 3 seconds after partner stops
    - Context menu has Reply, React, Delete options
    - All animations match UI-SPEC Animation Contract durations
    - `npm run build` succeeds
  </acceptance_criteria>

  <done>
    Emoji reactions with picker, reaction chips with counts, and typing indicators are fully functional. All interactions are animated per the UI-SPEC Animation Contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (reactions) | Reaction inserts cross to DB; RLS enforces user_id = auth.uid() |
| client → Supabase (typing) | Typing status updates cross to DB; RLS enforces pair-scoped access |
| client → local state | Context menu and delete dialog operate on local state before DB confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | reactions table | medium | mitigate | RLS ensures user_id = auth.uid(); UNIQUE constraint prevents duplicate reactions |
| T-02-07 | Info Disclosure | typing_status | low | accept | Typing status only reveals whether a user is typing in their own pair; no sensitive data |
| T-02-08 | Tampering | deleteMessage | high | mitigate | RLS ensures sender_id = auth.uid() for DELETE; only message author can delete |
| T-02-09 | Denial of Service | ReactionPicker | low | accept | 8 emojis per message is bounded; no unbounded growth |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run build` passes
- `npm run test:run` passes
- All component elements verified via automated check
- Reply, reaction, delete, and typing features work end-to-end
</verification>

<success_criteria>
- Users can reply to messages with inline quoting
- Users can react to messages with 8 emoji options, reactions display with counts
- Users can delete messages for self or everyone with confirmation
- Typing indicator shows when partner is composing
- All interactions are animated per UI-SPEC
- No regressions from Plan 02-01
</success_criteria>

<output>
Create `.planning/phases/02-real-time-chat/02-02-SUMMARY.md` when done
</output>
